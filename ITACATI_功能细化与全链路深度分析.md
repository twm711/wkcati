# ITACATI / CATI 功能细化 · 菜单关联 · 全链路深度分析

| 项目 | 内容 |
| --- | --- |
| 文档定位 | 《南康科技_ITACATI_NK3C_开发文档.md》的深化篇：功能细化到操作级、菜单间关联走查、CATI 全链路（端到端）分析 |
| 配套文件 ① | `ITACATI_问卷设计器_交互原型.html` —— 问卷设计器可操作交互稿 |
| 配套文件 ② | `ITACATI_坐席端_外呼作答原型.html` —— 坐席外呼作答可操作交互稿（签入/派样/呼叫/作答/结果码/样本去向全流程模拟） |
| 配套文件 ③ | `ITACATI_督导端_监控墙原型.html` —— 监控墙交互稿（坐席卡墙/监听/插话/四类强控/配额报警，内置话务仿真引擎） |
| 配套文件 ④ | `ITACATI_IVR设计器_交互原型.html` —— IVR 流程设计器交互稿（节点树画布/按键分支/来电模拟器） |
| 配套文件 ⑤ | `ITACATI_NK3C_数据库建表.sql`（MySQL 8.0 基准）+ `_Oracle版.sql` / `_SQLServer版.sql`（自动迁移稿，含核对清单） |
| 配套文件 ⑥ | `ITACATI_NK3C_测试用例集.xlsx` —— 96 条测试用例（P0×41），对应第 5 章验收清单 |
| 配套文件 ⑦ | `autotest/` —— 自动化测试骨架：test_logic_algorithms.py（22 条，已通过）/ test_prototype_smoke.py（Playwright 驱动 4 份原型）/ test_db_dispatch.py（真实 MySQL 并发） |
| 配套文件 ⑧ | `ITACATI_NK3C_API接口规格.yaml`（OpenAPI 3.0，47 端点，已通过校验）+ `ITACATI_NK3C_API接口说明.md`（全局约定/端点总表/原型-接口映射/错误码） |
| 配套文件 ⑨ | `index.html` 原型与文档中心（总导航）；`南康科技_ITACATI_NK3C_开发文档_合并版.docx`（打印评审版） |
| 阅读方法 | 第 2 章按菜单看功能；第 3 章看菜单之间如何串联；第 4 章看一条电话链路从底层到报表的完整轨迹 |
| 标注 | 【S】公开资料（见主文档参考资料）；【推定】行业通行设计重构；表名/字段名均对应配套 SQL |

---

## 0. 文档三角对照关系

```
   功能细化（本文件第2章）  ←—菜单编号 M01~M15—→  菜单资源表 sys_resource（SQL 初始化）
          ↑                                            ↑
          │功能点引用字段                                │视图/存储供数
          ↓                                            ↓
   全链路分析（本文件第4章）  ←—数据轨迹—→  话务/答卷/样本表（SQL 第 06/07/04 节）
```

- 开发排期：按第 5 章验收清单逐菜单交付；
- 测试用例：按第 4.6 节数据轨迹逐步断言；
- 原型：问卷设计器原型实现了 M04.2 的全部一级交互。

---

## 1. 功能编号体系

| 层级 | 格式 | 示例 |
| --- | --- | --- |
| 模块（一级菜单） | M + 两位序号 | M03 = 项目管理 |
| 子菜单（二级） | M + 模块号.序号 | M03.1 = 项目列表 |
| 功能点 | F + 模块号 + 两位序号 | F0305 = 项目启动/暂停 |
| 业务规则 | R + 功能号.n | R0305.1 |

角色缩写：**DA**=域管理员 domainAdmin；**OA**=组织管理员 orgAdmin；**GA**=组管理员（督导/质检）groupAdmin；**PA**=一线员工 phoneAdmin【S：NK3C 四类权限】。

---

## 2. 模块功能细化（按菜单）

### 2.1 M01 工作台

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0101 | 待办卡片 | /workspace | 全部 | 试访任务、待审答卷数、待处理工单数、即将到期预约；点击直达对应列表页并自动带入筛选条件 | 聚合查询 | M03/M04/M09/M13 |
| F0102 | 项目进度卡 | /workspace | OA/GA | 我负责/我组织的项目：`v_project_progress` 视图取数，显示样本消耗、成功率、配额进度 | 视图 | M08 |
| F0103 | 坐席在线看板 | /workspace | OA/GA | 在线/示忙/通话中人数、今日累计话务；从坐席状态服务器（nagentstateserver）内存态取数 | cti_agent_state_log | M07/M08 |
| F0104 | 公告与快捷入口 | /workspace | DA | 系统公告发布；常用功能快捷方式自定义 | sys_param | M15 |

### 2.2 M02 组织权限

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0201 | 域管理 | M02.1 | DA | 域 CRUD、停用（级联告警）、绑定物理库标识 db_tag（多层数据存储） | org_domain | M03.3 |
| F0202 | 组织管理 | M02.2 | DA/OA | 组织树维护；跨组织样本共享开关（同一公司不同项目共享样本资料【S】） | org_org | M05 |
| F0203 | 组管理 | M02.3 | OA/GA | 坐席组/访问员组/技能组维护；技能标签（方言）录入，供派样匹配 | org_group | M05.4 |
| F0204 | 员工管理 | M02.4 | OA/GA | 员工档案 CRUD；**工号从工号段取号且不可重复**【S】；绑定 CTI 软电话工号 agent_no；停用即强制签出 | sys_user | M07.2 |
| F0205 | 角色与资源授权 | M02.5 | DA | 资源树（菜单/按钮/API/权限四类）勾选授权；前端按 resource_code 控权（domainAdmin 等 4 类权限码【S】） | sys_role, sys_resource, sys_role_resource, sys_user_role | 全部 |
| F0206 | 操作日志 | M02.6 | DA | 登录日志 + 操作审计（模块/动作/前后值 JSON），支持导出 | sys_login_log, sys_op_log | M15 |

**业务规则**
- R0204.1：删除员工为逻辑停用；有历史话务记录的工号永不复用。
- R0205.1：权限校验发生在控制器层（统一入口），按钮级由前端 Code 判断 + 后端二次校验（防越权）。

### 2.3 M03 项目管理

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0301 | 项目列表 | M03.1 | OA/GA | 多条件筛选（状态/类型/日期/组织）；列表显示配额进度与成功率（视图取数）；行操作：配置/复制/分发/导出 | prj_project | 全部 |
| F0302 | 新建项目向导 | M03.2 | OA | 五步向导：①基本信息（类型/起止/项目时间段）→②选问卷（仅 PUBLISHED）→③选样本/抽样方案→④选呼叫策略→⑤分配坐席组+配额；提交后项目为 DRAFT | prj_project, prj_project_group | M04/M05/M06 |
| F0303 | 项目复制/模板 | M03.1 | OA | 复制项目骨架（问卷引用可选升级为新版本）；另存为模板 | prj_project(is_template) | M04.6 |
| F0304 | 分发管理 | M03.3 | DA/OA | 母项目分发到子数据库/子公司生成子项目；母/子项目操作权限不同；上级可汇总下级数据【S】 | prj_distribution | M02.1 |
| F0305 | 项目控制 | M03.1 | OA | 启动（前置校验：问卷已发布、样本≥阈值、策略时段合法、坐席组已配）、试访↔正式切换、暂停/恢复、终止 | prj_project.status | M04/M05/M06/M07 |
| F0306 | 项目排班 | M03.4 | GA | 按日/组排班，设定每班计划坐席数；排班联动允许呼出时段 | prj_schedule | M06.4 |
| F0307 | 项目归档 | M03.5 | OA | 结项→归档：只读化；归档前强制完成答卷审核与导出归档包 | prj_project.status | M09/M11 |

**业务规则**
- R0305.1：状态机约束见 4.2.1，非法跳转在服务层拒绝并写 sys_op_log。
- R0305.2：试访答卷打 `TESTING` 标记，不计入正式统计（单题/交叉统计默认过滤）。
- R0304.1：分发项目问卷版本锁定，子库不可改题干（保证数据可汇总）。

### 2.4 M04 问卷管理（原型已实现核心交互）

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0401 | 问卷列表 | M04.1 | OA | 版本管理（发布一次版本 +0.1）、状态（草稿/试测/已发布/下线）、复制、版本对比 | qnr_questionnaire | M03.2 |
| F0402 | 问卷设计器 | M04.2 | OA | 左题型/题库 + 中所见即所得画布 + 右属性面板（原型交互稿即此页）；20 种题型【S】；题目增删排序复制；逐选项跳转；必答；选项随机/轮换/倒序【S】；整体/单题字体【S】；题干/选项多媒体 | qnr_question, qnr_option | M04.3~4.6 |
| F0403 | 逻辑与配额 | M04.3 | OA | 高级逻辑：条件显示/条件跳转/校验（多维条件约束【S】）、选项关联/互斥/排斥；单重与交叉配额定义（目标量、超配放行策略）【S】 | qnr_logic, qnr_quota, qnr_quota_cell | M08.1 |
| F0404 | 校验调试 | M04.4 | OA | 自动纠错【S】：题干空、选项不足、跳转失效、**不可达死题分析（BFS）**、数值范围矛盾；模拟作答调试器逐步执行 | 内存校验报告 | M04.2 |
| F0405 | 题库管理 | M04.5 | OA | 常用题库/自建题库分类维护、拖选使用【S】；使用次数统计 | qnr_bank | M04.2 |
| F0406 | 模板管理 | M04.6 | OA | 问卷模板、页面风格模板 | qnr_template | M04.2 |
| F0407 | 导入/导出/打印 | 工具栏 | OA | doc/xml/sur 多格式导入导出【S】；打印排版（原型已实现 print CSS） | qnr_import_export_log | — |
| F0408 | 发布 | 工具栏 | OA | 发布前强制逻辑校验通过；发布后生成不可变版本快照；**执行中项目允许小幅调整，坐席端自动更新、答卷按 qnr_version 归档**【S】 | qnr_questionnaire.status | M03.5 |

### 2.5 M05 样本管理

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0501 | 样本池 | M05.1 | OA/GA | 全量样本查询；状态/批次/组别筛选；行级明细（多号码+接触历史） | smp_sample, smp_phone | M07.3 |
| F0502 | 样本导入 | M05.2 | OA | Excel/CSV 上传→字段映射（含自定义字段）→查重（号码域内唯一）→校验→入库报告；导入即生成批次号 | smp_sample, smp_phone, smp_field_def | M05.5 |
| F0503 | 随机抽样 | M05.3 | OA | ①按规则随机生成电话号码（号段/区号约束）【S】；②系统抽样/分层抽样（region_tag）；③两阶段抽样（案例：Mitofsky-Waksberg【S】）；生成后自动"打散"（shuffle_key 随机化） | smp_sample | — |
| F0504 | 样本分配 | M05.4 | OA/GA | 按条件批分配到坐席/访问员组（样本分级→不同访问员组【S】）；回收（收回未呼样本）；公平性：按组样本均衡 | smp_sample.owner_*, smp_assign_log | M02.3 |
| F0505 | 字段与字典 | M05.5 | OA | 自定义字段定义（TEXT/NUMBER/DATE/DICT）；字典表维护【S】；字段批量修改【S】 | smp_field_def, smp_dict | M05.2 |
| F0506 | 黑名单/半年原则 | M05.6 | OA | 黑名单维护（全局/项目级；拒访、空号自动入列可配置）；半年原则参数（默认 180 天【S】）；生效范围说明与试查工具 | smp_blacklist, sys_param | M06 |
| F0507 | 状态码表 | M05.7 | OA | 自定义每个电话结束状态【S】：类别（成功/失败/中性/预约）、是否闭环、是否允许重拨、是否自动入黑名单 | smp_status_code | M07/M09 |

### 2.6 M06 呼叫策略

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0601 | 策略列表 | M06.1 | OA | 策略 CRUD；绑定项目；启用/停用 | strat_strategy | M03.2 |
| F0602 | 预测拨号参数 | M06.2 | OA | 拨号倍率初值、**呼损率上限**【S】、无效号码自动筛除开关（空号/传真/占线/无人接听【S】）；运行期由算法自调（见 4.5.1） | strat_strategy | M08.1 |
| F0603 | 重拨规则 | M06.3 | OA | 最大次数/最小间隔/换号规则（一人多号按序呼叫【S】）；与状态码 allow_redial 联合生效 | strat_strategy, smp_status_code | M05.7 |
| F0604 | 时段与排班 | M06.4 | OA/GA | 每周允许时段 + 节假日禁呼；与项目排班联动；时段外策略自动挂起 | strat_time_window, prj_schedule | M03.6 |
| F0605 | 主叫号码管理 | M06.5 | OA | 项目主显号；**为访问员单独设置主叫号码**【S】 | strat_caller_assign | M02.4 |
| F0606 | 呼入路由 | M06.6 | OA/GA | 呼入队列、技能组路由（按技能标签匹配）、溢出/夜间转 IVR 规则 | strat_time_window+ivr_flow | M14 |

### 2.7 M07 执行管理（坐席作业面）

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0701 | 坐席签入/签出 | 坐席端 | PA | 浏览器登录 + 软电话（NKZXAgent【S】）签入；签入写状态流水；签出前必须无进行中话务 | cti_agent_state_log | M02.4 |
| F0702 | 状态切换 | 坐席端 | PA | 示闲/示忙（原因码）/小休；示忙期间不参与派样与预测拨号统计 | cti_agent_state_log | M08.2 |
| F0703 | 派样引擎（后台服务） | — | 系统 | 取号 SQL 见 SQL 文件附录：状态 IDLE + 半年原则 + 黑名单 + 重拨上限 + `FOR UPDATE SKIP LOCKED` 派样锁；派样即置 ASSIGNED 并写流水；坐席断线超时自动回收（参数 sample.lock.seconds） | smp_sample, smp_assign_log | M05/M06 |
| F0704 | 外呼作答页 | 坐席端 | PA | 弹屏三区布局（样本信息/问卷/结果码）；自动跳题与校验（问卷引擎）；作答实时入库；挂机后提交结果码 | cti_call_record, ans_sheet, ans_answer | M04/M09 |
| F0705 | 预约登记 | 坐席端 | PA | 采集预约时间/号码→生成预约任务；到点前在坐席端提醒 | ans_appointment | M07.4 |
| F0706 | 呼入应答页 | 坐席端 | PA | 来电弹屏（主叫匹配样本/历史工单）→左侧客户资料+历史接触，右侧业务引导问卷（一套问卷首题分流【S】）→可生成工单 | wfm_workorder | M13 |
| F0707 | 执行总览 | M07.1 | GA | 在线坐席数、排队/派样队列、今日话务、接通率实时数字屏 | v_agent_today | M08 |
| F0708 | 预约与回拨队列 | M07.4 | GA/PA | 到期预约列表、一键回拨、过期自动标记 EXPIRED | ans_appointment | — |

### 2.8 M08 实时监控

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0801 | 项目监控 | M08.1 | OA/GA | 配额进度条（实时）、成功/失败/接通率曲线、时段话务图；数据源：内存态 + rpt 表增量 | v_quota_progress, cti_call_record | M03/M04.3 |
| F0802 | 坐席监控墙 | M08.2 | GA | 坐席卡片墙（状态/通话时长/当前项目样本）；点选坐席可执行：**监听、插话、强制示忙、强制示闲、强制挂断、强制签出**【S】、发消息【S】 | cti_monitor_event | M07 |
| F0803 | 报警提醒 | M08.3 | OA/GA | 规则：配额将满（≥95%）、成功率异常波动、坐席长时间无通话、派样池见底；站内消息 + 声音提醒 | 内存规则 | — |
| F0804 | 监听/插话实现要点 | — | GA | 监听：CTI Proxy 下发 LISTEN（耳语旁听）；插话 BARGE 变三方；全程事件写 cti_monitor_event 供考核 | cti_monitor_event | M12 |

### 2.9 M09 答卷管理

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F0901 | 答卷列表 | M09.1 | GA | 项目/坐席/结果码/时长/状态筛选；打开进入审核页 | ans_sheet | — |
| F0902 | 答卷审核 | M09.2 | GA | **录音回放与答卷逐题同步比对**【S】（播放进度↔题目高亮联动）；动作：通过/驳回（退回重访）/作废；批量审核【S】；审核指标设置（NK3C【S】：时长阈值、必答题完整性、作答速度） | ans_sheet.status, audit_* | M12 |
| F0903 | 答卷编码 | M09.3 | GA | 开放题答案列表→机编候选（AUTO）+ 人工赋码→码表管理；编码进度与双人一致性复核 | ans_code | M10.3 |
| F0904 | 录音管理 | M09.4 | GA | 录音检索试听、另存/导出【S】、留存期清理策略（sys_param: recording.retention.days） | cti_recording | M11.2 |

### 2.10 M10 统计报表

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F1001 | 话务统计 | M10.1 | OA/GA | 呼出量/接通率/平均时长/坐席效率/时段分布；单项目、多项目口径【S】 | rpt_day_project, rpt_day_agent | — |
| F1002 | 样本统计 | M10.2 | OA/GA | 状态分布、黑名单命中、预约转化、批次对比 | smp_sample 聚合 | — |
| F1003 | 单题统计 | M10.3 | OA/GA | 按题频数/百分比**实时查看（无需导出）**【S】；开放题按编码统计 | rpt_question_stat / 实时聚合 | M09.3 |
| F1004 | 交叉统计 | M10.4 | OA/GA | 任意两题及以上交叉表；与样本字段（性别/区域）交叉【S】；卡方检验提示【推定】 | 实时聚合 | — |
| F1005 | 配额统计 | M10.5 | OA/GA | 配额完成度实时表（v_quota_progress） | v_quota_progress | M04.3 |
| F1006 | 话费统计 | M10.6 | OA | **自定义话费规则**（费率/时段/计费口径）并统计【S】 | 计算列 | M06.5 |
| F1007 | 坐席绩效 | M10.7 | OA/GA | 话务量/成功率/质检分/在线时长综合排名（绩效考核属 NK3C 全流程【S】） | rpt_day_agent, qc_result | M12 |

### 2.11 M11 数据导出

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F1101 | 答卷数据导出 | M11.1 | OA | 格式：**Excel / SPSS(.sav) / Quantum / Txt**【S】；范围（项目/结果码/时间）；携带变量字典（SPSS 元数据：题干=变量标签，选项=值标签）；按项目整体导出【S】 | exp_task | M09 |
| F1102 | 录音批量导出 | M11.2 | OA/GA | 按项目/坐席/时间打包 ZIP；附 CSV 索引 | exp_task | M09.4 |
| F1103 | 账号/配额导出 | M11.1 | DA/OA | 管理与话务员账号导出【S】；项目配额条件导出【S】 | exp_task | — |
| F1104 | 导出任务中心 | M11.3 | OA | 异步任务队列、进度、下载；**导出审批**（个人信息保护，见主文档第 15 章）；导出行为全量审计 | exp_task | M02.6 |

### 2.12 M12 质检管理

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F1201 | 质检任务 | M12.1 | GA | 抽检规则：按比例/指定坐席/指定项目自动生成任务 | qc_task | — |
| F1202 | 录音质检 | M12.2 | GA | 播放录音+评分表打分+缺陷标记；致命项一票否决 | qc_result | M09.4 |
| F1203 | 答卷质检/工单质检 | M12.3 | GA | 与答卷审核联动（答卷审核指标【S】）；工单内容合规检查 | qc_result | M09.2/M13 |
| F1204 | 评分表管理 | M12.4 | OA/GA | 评分模板 CRUD、权重、致命项配置 | qc_template(_item) | — |
| F1205 | 质检报告 | M12.5 | OA/GA | 坐席成绩排名、缺陷类型分布、申诉处理流（appeal_status） | qc_result | M10.7 |

### 2.13 M13 工单管理

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F1301 | 工单列表 | M13.1 | GA/PA | 多渠道来源（电话/微信/微博/短信/邮件/WebChat【S】）统一列表；SLA 超时高亮 | wfm_workorder | — |
| F1302 | 字段与布局 | M13.2 | OA | **自定义工单字段、自定义字段布局**【S】 | wfm_field_def | — |
| F1303 | 工作流模板 | M13.3 | OA | **工作流模板选择及节点设置**【S】：受理→处理→回访→归档；流转条件、超时升级 | wfm_template, wfm_node | — |
| F1304 | 工单分派 | M13.4 | GA | 手动/规则（轮询/按组）分派、改派、催办 | wfm_workorder.assignee | M02.3 |
| F1305 | 知识库 | M13.5 | OA/GA | 业务知识条目维护；坐席呼入页弹窗检索辅助 | 知识库表（可复用 smp_dict 结构扩展） | — |
| F1306 | 回访联动 | M13.1 | GA | 工单结案自动生成回访外呼任务（客户服务+主动回访两大平台联动【S】） | ans_appointment/prj_project | M03/M07 |

### 2.14 M14 IVR 管理

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F1401 | 流程列表 | M14.1 | OA | 流程与呼入号码绑定、版本、上下线 | ivr_flow | M06.6 |
| F1402 | 流程设计器 | M14.2 | OA | 节点画布：播报/按键菜单/外部查询接口/留言/转人工/条件/时段；**任意级数、任意回合跳转**【S】 | ivr_node | — |
| F1403 | 语音文件 | M14.3 | OA | 提示音上传、TTS 文本合成、时长校验 | ivr_audio | — |
| F1404 | 留言管理 | M14.4 | GA | 留言试听、标记处理、**一键转工单** | ivr_message | M13 |
| F1405 | 自助查询对接 | 节点参数 | OA/DA | QUERY 节点对接业务系统（案例：对接不动产登记信息管理平台查询办证结果【S】） | param_json | M15.5 |

### 2.15 M15 系统维护

| 编号 | 功能 | 菜单入口 | 角色 | 功能说明与业务规则 | 落表 | 关联 |
| --- | --- | --- | --- | --- | --- | --- |
| F1501 | 参数配置 | M15.1 | DA | 半年原则天数、重拨上限、呼损率、录音留存、派样锁等全部 sys_param | sys_param | 全局 |
| F1502 | 数据字典 | M15.2 | DA | 全局码表（结果码/状态码/行业码） | smp_dict, smp_status_code | M05.7 |
| F1503 | 备份恢复 | M15.3 | DA | 库备份策略、录音归档、清理任务；多重数据安全备份机制【S：NK3C】 | 运维任务 | — |
| F1504 | 许可证管理 | M15.4 | DA | 坐席数授权导入校验（产品按坐席授权）【推定】 | license 表 | — |
| F1505 | 接口管理 | M15.5 | DA | 对外 API（查询/推送）与第三方系统对接配置、鉴权密钥轮换 | api 配置表 | M14.5 |

---

## 3. 菜单关联分析（流程 × 菜单）

### 3.1 菜单编号总表（与 SQL `sys_resource` 初始化数据一致）

| 编号 | 菜单 | 子菜单 | 编号 | 菜单 | 子菜单 |
| --- | --- | --- | --- | --- | --- |
| M01 | 工作台 | — | M09 | 答卷管理 | 列表/审核/编码/录音 |
| M02 | 组织权限 | 域/组织/组/员工/授权/日志 | M10 | 统计报表 | 话务/样本/单题/交叉/配额/话费/绩效 |
| M03 | 项目管理 | 列表/向导/分发/排班/归档 | M11 | 数据导出 | 数据/录音/任务 |
| M04 | 问卷管理 | 列表/设计器/逻辑配额/校验/题库/模板 | M12 | 质检管理 | 任务/录音/答卷/评分表/报告 |
| M05 | 样本管理 | 池/导入/抽样/分配/字段/黑名单/状态码 | M13 | 工单管理 | 列表/字段/流程/分派/知识库 |
| M06 | 呼叫策略 | 列表/预测/重拨/时段/主叫/呼入路由 | M14 | IVR管理 | 流程/设计器/语音/留言 |
| M07 | 执行管理 | 总览/坐席状态/派样/预约 | M15 | 系统维护 | 参数/字典/备份/许可/接口 |
| M08 | 实时监控 | 项目/监控墙/报警 | | | |

### 3.2 菜单间跳转与数据流图

```
                    ┌───────────────【配置期】───────────────┐
                    │                                        │
  M02 组织权限 ──组/技能──┐                          M04 问卷管理
  （坐席组/技能标签）      │                        设计→校验→配额→发布
                          ↓                              │ questionnaire_id
                    M03 项目管理  ←───────────────────────┘
                    │   ↑
        ①选样本/抽样 │   │ ④绑定策略
                    ↓   │
  M05 样本管理          M06 呼叫策略
  导入/抽样/分配/黑名单 ──→ strategy_id ──→ 启动校验（F0305）
        │ sample_id                                  │
        │                                            ↓
        │                                    【执行期】M07 执行管理
        │                                     坐席签入→派样→外呼作答
        │                                            │
        │        M08 实时监控 ←—状态/话务事件—────────┤
        │        （项目进度/监控墙/监听插话/报警）        │ 通话/答卷实时落库
        │                                            ↓
        │                                    【收尾期】M09 答卷管理
        │                                     审核(录音比对)/编码
        │                                        │           │
        │                              M12 质检管理      M10 统计报表
        │                               抽检/评分/绩效 ←—成绩—→ 话务/单题/交叉/配额
        │                                        │           │
        │                                        └→ M10.7 绩效 │
        │                                                    ↓
        └──────────────────────────────────────  M11 数据导出（Excel/SPSS/Quantum/Txt）
                                                             │
                            M15 系统维护（参数/字典/备份/接口）贯穿全部 │
                            M13 工单管理 ←—呼入链路— M14 IVR ←— M06.6 呼入路由
```

### 3.3 菜单 × 数据对象 × 动作矩阵（C=Create R=Read U=Update X=Export）

| 菜单 \ 对象 | project | questionnaire | sample | strategy | call_record | sheet/answer | recording | quota | workorder | ivr |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M03 项目管理 | CRU | R(选绑) | R(选绑) | R(选绑) | R | R | — | RU | — | — |
| M04 问卷管理 | — | CRUD | — | — | — | — | — | CRUD | — | — |
| M05 样本管理 | R | — | CRU | — | — | — | — | — | — | — |
| M06 呼叫策略 | R | — | R(过滤条件) | CRUD | — | — | — | — | — | R |
| M07 执行管理 | R | R(作答) | RU(派样/回收) | R | CRU | CR | C | — | C(呼入) | — |
| M08 实时监控 | R | R | R | R | R+强控 | R | R(监听) | RU(done_count) | — | — |
| M09 答卷管理 | R | R | R | — | R | RU(审核/编码) | RX | — | — | — |
| M10 统计报表 | R | R | R | — | RX | RX | — | R | RX | — |
| M11 数据导出 | R | R(字典) | RX | — | RX | RX | RX | RX | RX | — |
| M12 质检管理 | R | — | — | — | R | RU(评分) | R | — | R | — |
| M13 工单管理 | R | — | R(客户) | — | R(来源) | — | R(质检留证) | — | CRUD | — |
| M14 IVR管理 | — | — | — | R | R(留言来电) | — | R(留言) | — | C(转工单) | CRUD |

> 矩阵用途：新开发排模块时，按列汇总即得该数据对象的完整读写入口，用于设计服务接口与权限点。

### 3.4 主流程菜单走查（逐步：在哪个菜单、做什么、产出什么、流向哪里）

**流程 A：外呼电话调查（CATI 主链）**

| 步 | 菜单 | 操作 | 产出/数据 | 流向 |
| --- | --- | --- | --- | --- |
| A1 | M02.3/2.4 | 建坐席组、录员工（含技能） | org_group, sys_user | M03.2⑤ / 派样匹配 |
| A2 | M04.2 | 设计问卷 → M04.4 校验 → M04.3 配额 → M04.8 发布 | qnr_* | M03.2② |
| A3 | M05.2/5.3 | 导入样本或随机抽样 → 打散 | smp_sample/phone | M03.2③ |
| A4 | M05.6/5.7 | 确认黑名单、半年原则参数、状态码表 | smp_blacklist, sys_param, smp_status_code | 派样过滤 |
| A5 | M06.1 | 建策略：模式/呼损率/重拨/时段/主显号 | strat_* | M03.2④ |
| A6 | M03.2 | 五步向导建项目并绑定 2/3/5 的成果 | prj_project(+group) | M03.1 |
| A7 | M03.1 | 启动（先试访→正式） | status=RUNNING | M07 全部坐席可见 |
| A8 | M07 坐席端 | 签入→派样→外呼→弹屏作答→结果码 | cti_call_record, ans_* | M08/M09 |
| A9 | M08.1/8.2 | 盯进度与配额、监听/插话/强控、发消息 | cti_monitor_event | 纠偏执行 |
| A10 | M09.2/9.3 | 录音比对审核、开放题编码 | ans_sheet.status, ans_code | M10/M11 |
| A11 | M10.1~10.5 | 话务/样本/单题/交叉/配额报表 | 聚合结果 | 项目例会 |
| A12 | M11.1/11.3 | 导出 Excel/SPSS/Quantum/Txt（审批） | exp_task → 文件 | 交付结项 |
| A13 | M03.5 | 归档（只读） | status=ARCHIVED | M15 备份 |

**流程 B：呼入客服（IVR + 工单）**

| 步 | 菜单 | 操作 | 产出 | 流向 |
| --- | --- | --- | --- | --- |
| B1 | M06.6 | 配置呼入路由、技能组 | 路由规则 | 语音网关 |
| B2 | M14.1/14.2 | 设计发布 IVR（查询/咨询/投诉三分支） | ivr_flow/node | 来话入口 |
| B3 | M14.3 | 上传/TTS 生成提示音 | ivr_audio | IVR 播报 |
| B4 | 来话 | 客户按键：自助查询（QUERY 对接业务系统【S】）或转人工 | ivr 话务 | M07.6 弹屏 |
| B5 | M07.6 | 坐席应答弹屏→业务引导问卷→录工单 | wfm_workorder | M13.1 |
| B6 | M13.4 | 分派→处理→（超时升级 SLA） | wfm_action_log | M13.1 |
| B7 | M13.1 | 结案自动生成回访任务 | ans_appointment | M07.8 回拨 |
| B8 | M12.3 | 工单质检 | qc_result | M10.7 绩效 |

**流程 C：质检闭环**

M12.1 生成任务（按比例抽单）→ M12.2/M12.3 打分 → 不合格走 M09.2 驳回重访 / M12.5 申诉 → 成绩进 M10.7 绩效 → 督导针对低分项在 M08.2 加强监控/发消息辅导。

### 3.5 角色 × 菜单权限矩阵

| 菜单 | DA 域管理员 | OA 组织管理员 | GA 督导/质检 | PA 坐席 |
| --- | :-: | :-: | :-: | :-: |
| M01 工作台 | ✓ | ✓ | ✓ | ✓(精简) |
| M02 组织权限 | ✓(全部) | ✓(组织/组/员工) | ✓(组/员工只读) | ✗ |
| M03 项目管理 | ✓ | ✓ | 只读 | ✗ |
| M04 问卷管理 | ✓ | ✓ | 只读 | ✗ |
| M05 样本管理 | ✓ | ✓ | 分配/回收 | ✗ |
| M06 呼叫策略 | ✓ | ✓ | 时段/排班 | ✗ |
| M07 执行管理 | 只读 | 只读 | ✓ | ✓(坐席面) |
| M08 实时监控 | 只读 | ✓ | ✓(监听/强控) | ✗ |
| M09 答卷管理 | 只读 | ✓ | ✓ | 仅本人答卷 |
| M10 统计报表 | ✓ | ✓ | ✓ | 仅本人绩效 |
| M11 数据导出 | 审批 | ✓ | 录音导出 | ✗ |
| M12 质检管理 | 只读 | ✓ | ✓ | 申诉入口 |
| M13 工单管理 | — | ✓ | ✓/分派 | ✓(本人工单) |
| M14 IVR管理 | ✓ | ✓ | 只读 | ✗ |
| M15 系统维护 | ✓ | ✗ | ✗ | ✗ |

---

## 4. CATI 全链路深度分析

### 4.1 端到端链路总图（从 PSTN 到报表）

```
 被访者电话                      坐席
    │ PSTN/中继                   │ 耳机+软电话(NKZXAgent)
    ▼                            ▼
┌─────────┐  信令/话路   ┌──────────────┐   指令翻译   ┌──────────────────┐
│ 语音网关  │◄──────────►│  CTI Server   │◄──────────►│  CTI Proxy(平台无关)│
│ /PBX/板卡 │            │ (排队/路由/会议)│            │  统一业务指令层【S】  │
└─────────┘            └──────┬───────┘            └────────┬─────────┘
                              │ 话务事件(振铃/接通/挂断)        │ 派样/弹屏/状态
                              ▼                             ▼
                    ┌──────────────────┐          ┌────────────────────┐
                    │ 录音服务           │          │ nagentstateserver    │
                    │ (旁录/双轨/存储)    │          │ (坐席状态/监控事件/消息)│
                    └────────┬─────────┘          └─────────┬──────────┘
                             │ recording_id                  │ WS 推送
                             ▼                               ▼
        ┌────────────────────────────────────────────────────────────────┐
        │                     nweb（应用服务层）                            │
        │  派样引擎│问卷引擎│配额引擎│审核流│报表聚合│导出队列│IVR引擎│工单流    │
        └───────┬──────────────────┬───────────────────┬─────────────────┘
                ▼                  ▼                   ▼
        ┌──────────────┐  ┌──────────────┐   ┌──────────────────────┐
        │ 样本池(独立服务)│  │ DB 主从       │   │ Redis(坐席态/配额计数/  │
        │ 【S:独立样本池】│  │ Oracle/MSSQL │   │ 派样锁/实时监控数)       │
        └──────────────┘  │ /MySQL+录音库  │   └──────────────────────┘
                          └──────────────┘
```

**关键认识（公开资料还原）**：
1. 业务与媒体平台解耦（CTI Proxy）【S】——同一套业务可跑在交换机、语音板卡或 VoIP 网关上；
2. 独立样本池避免客户端直连数据库，保障大并发【S】；
3. 消息中心是各子系统的事件枢纽（坐席状态、消息、监控事件）【S】。

### 4.2 六大状态机（全链路的骨架）

**4.2.1 项目 prj_project.status**

```
DRAFT ──启动校验通过──► TESTING ──切正式──► RUNNING ⇄ PAUSED
  ▲                      │                   │
  │复制/改配               │试访发现问卷问题      ├────终止────► FINISHED ──归档──► ARCHIVED
  └──────────────────────┴───────────────────┘
启动校验：问卷=PUBLISHED 且 样本≥阈值 且 策略时段合法 且 坐席组非空
```

**4.2.2 问卷 qnr_questionnaire.status**

```
DRAFT ──校验通过──► TESTING(试访) ──确认──► PUBLISHED ──下线──► OFFLINE
  ▲                                        │
  └──────── 修改副本(新版本) ◄── 执行中微调：原版本不变，坐席端热更新【S】
```

**4.2.3 样本 smp_sample.status**（核心）

```
          ┌──────────────────────────────────────────────┐
          │            ┌─── 结果码 closed=0(可重拨) ──────┐│
          ▼            ▼                                ││
IDLE ──派样锁──► ASSIGNED ──外呼──► INCALL ──挂机──► 按结果码判定：
  ▲               │断线超时回收                     │ ├─ SUCCESS(闭环)
  │               └──► 回 IDLE (attempts+1)        │ ├─ FAIL 闭码(REFUSE/INVALID…→可能入黑名单)
  │                                                │ ├─ FAIL 开码(NA/BUSY…→回池重拨队列)
  └──────────────── RECYCLED ◄─────────────────────┤ ├─ APPOINT ──► 到期回拨 ──► INCALL
                     (整批收回)                      │ └─ PARTIAL ─► 驳回重访走 ASSIGNED
                                                    └─ BANNED(黑名单, 终态)
```

**4.2.4 坐席（软电话状态）**

```
LOGOUT ──签入──► LOGIN(Not Ready) ──示闲──► READY ⇄ AUX(示忙,原因码)
                    ▲                        │ │
              强制签出/签出                    │ ▼
                    └──── 挂机 ◄── ACW(后处理) ◄─ BUSY(通话)
派样只面向 READY 坐席；预测拨号按 READY 数与呼损率估算拨号量。
```

**4.2.5 话务 cti_call_record / 答卷 ans_sheet**

```
CALL:  INIT(取号) ─► DIALING ─► RINGING ─► CONNECTED ─► wrap up(后处理) ─► CLOSED(落结果码)
                        │           │
                        └─ FAIL类    └─ 呼损(abandon): 接通但无坐席→放弃并计数
SHEET: DOING ─提交─► SUBMITTED ─审核通过─► AUDITED
         │作答中断线→断点续答(样本仍锁)   ├─驳回─► REJECTED(生成重访)
         └—未提交挂机─► VOID(残卷)      └─作废─► VOID
```

**4.2.6 工单 wfm_workorder.status**

```
OPEN ─受理─► PROCESSING ──► WAITING(回访) ──回访通过──► CLOSED
  │              │ 超时SLA→升级/改派        │回访不满意
  └─CANCELED     └────────────────────────►PROCESSING(重开)
```

### 4.3 关键时序（五条）

**4.3.1 预测拨号（PREDICTIVE）**

```
派样引擎        CTI Proxy        语音网关        nagentstateserver     坐席
  │计算拨号量N= f(READY坐席数,历史接通率,呼损率上限)                       │
  ├─批量取号(跳过锁)►│originat N路        │                                │
  │                ├────外呼──────────►│                                 │
  │                │                   ├─空号/传真音检测──►标记INVALID/FAX │
  │                │◄──接通事件─────────│                                 │
  │                ├─找READY坐席────────┼──────────────────────────────►│弹屏
  │                │  (无空闲→接通即弃→abandon+1, 反馈调节N)               │样本+问卷
  │                │                   │          │通话中状态/监听事件     │作答实时入库
  │                │◄──挂机─────────────┼──────────┼────────────────────►│提交结果码
  │样本回池/闭环◄───┤ 结果码入 cti_call_record │ 状态回 READY
```

**4.3.2 预览呼出（PREVIEW）**：坐席端展示样本摘要 → 坐席点"呼叫" → CTI Proxy 外呼 → 接通弹全量问卷；跳过样本回池（可配跳过上限）。

**4.3.3 呼入转工单**：来话 → 网关 → CTI 排队 → IVR(M14) 按键 → 转人工按技能路由 → 坐席弹屏(客户360) → 问卷引导 → 挂机 → 工单 OPEN(M13) → 工作流流转 → 结案生成回访。

**4.3.4 监听/插话**：督导(M08.2)点坐席 → nweb → nagentstateserver 鉴权(GA) → CTI Proxy LISTEN 建旁听Leg → 插话则变 MIXED 三方；全程写 cti_monitor_event；被监听坐席可配置收到提示音。

**4.3.5 答卷审核**：M09.2 打开答卷 → 并行取 ans_answer + cti_recording → 播放器与题目时间轴对齐(按每题 answered_at 偏移) → 驳回则生成重访任务(REJECTED→新 ASSIGNED)。

### 4.4 并发与一致性设计（高频踩坑点）

| # | 问题 | 设计 | 落地 |
| --- | --- | --- | --- |
| 1 | 同一样本被两个坐席同时拨出 | 派样锁 | `SELECT ... FOR UPDATE SKIP LOCKED` + owner_agent_id + 锁超时回收（sample.lock.seconds） |
| 2 | 配额超发（并发通过判定） | 原子扣减 | `UPDATE qnr_quota_cell SET done_count=done_count+1 WHERE id=? AND done_count<target_count`，0 行受影响即回滚放行逻辑（Redis 计数加速，DB 兜底） |
| 3 | 话务/答卷/录音三方不一致 | 事务边界+对账 | 挂机事件幂等落 call_record；sheet 引用 call_id；录音异步落库补 recording_id；定时对账任务修复孤儿录音 |
| 4 | 坐席断线作答丢失 | 本地草稿+断点续答 | 每题实时提交（answered_at 即落库），重连后按 sheet=DOING 续答；样本锁超时 > 常规断线重连窗口 |
| 5 | 问卷中途修改的版本混乱 | 版本快照 | ans_sheet 记 qnr_version；作答中坐席完成当前版本，新派样本用新版；统计按版本可切分【S：访员端自动更新】 |
| 6 | 重复点击/重复提交结果码 | 幂等键 | call_id + result_code 幂等；重复请求返回首写结果 |
| 7 | 预测外呼过量导致呼损超标 | 反馈调节 | 每分钟用实际接通率/放弃率修正 dial_ratio（见 4.5.1），超过上限强制降速 |

### 4.5 核心算法（伪代码级）

**4.5.1 预测拨号量**（每 T=5s 决策一次）：

```
输入: A=READY坐席数, ATT=近期平均通话时长, HT=近期平均后处理时长,
      CR=近期接通率, AB=近期呼损率, AB_max=呼损率上限
占用比 OCC = A × ATT / (ATT + HT)            # 服务占用模型(Erlang 近似)
N_dial = ceil( A × dial_ratio × CR )
若 AB > AB_max:      dial_ratio *= 0.8;  N_dial = floor(N_dial × 0.8)   # 降速
若 连续K周期 AB≈0 且 OCC<0.9: dial_ratio = min(dial_ratio×1.1, 上限)      # 提速
若 N_dial ≤ 0 或 时段窗外/排班外: N_dial = 0
```

**4.5.2 派样取号**（等价 SQL 见建表脚本附录）：过滤（项目+IDLE+半年原则+黑名单+重拨上限+时段）→ 按预约优先 → shuffle_key 随机序 → 技能匹配优先（组技能=样本 region_tag/language 需求）→ `LIMIT 1 SKIP LOCKED`。

**4.5.3 配额命中判定**：答卷提交时，逐题答案映射为配额条件键 → 命中 cell → 原子扣减；全部满格则该样本按 overflow 策略处理（放行/终止并标记 QUOTA_FULL）。

**4.5.4 半年原则**：`last_called_at < NOW() - halfyear.days`；重拨（未接通）是否刷新 last_called_at 可配置（行业通常"接通才刷新"）。

**4.5.5 抽样**：随机数生成按号段规则去黑名单去重；系统抽样按步长 k=N/n；分层按 region_tag 比例分配；两阶段（Waksberg）先抽住宅簇号再在簇内扩展【S：深圳案例】。

### 4.6 一通成功电话的完整数据轨迹（测试断言用）

| 时刻 | 动作 | 写入表/字段 |
| --- | --- | --- |
| t0 | 派样 | smp_sample: ASSIGNED, owner_agent_id; smp_assign_log: ASSIGN |
| t1 | 发起呼叫 | cti_call_record: INIT→DIALING（caller/called/strategy） |
| t2 | 振铃 | ring_duration 计时 |
| t3 | 接通弹屏 | connect_time; 坐席状态 BUSY（cti_agent_state_log）; 样本 INCALL |
| t4 | 开录音 | cti_recording 先占位（recording_id 回写 call_record） |
| t5 | 逐题作答 | ans_sheet: DOING; ans_answer 逐条 upsert（answered_at） |
| t6 | 配额命中 | qnr_quota_cell.done_count +1（原子） |
| t7 | 挂机 | end_time/talk_duration; 样本状态待定; 坐席 ACW |
| t8 | 提交结果码 SUCCESS | call_record.result_code; ans_sheet SUBMITTED+duration; smp_sample SUCCESS(闭环, last_called_at=now) |
| t9 | 录音就绪 | cti_recording 落 file_path/duration |
| t10 | 监控刷新 | 内存计数 → rpt_day_project/agent 增量（T+1s 内） |
| t11 | 审核 | ans_sheet AUDITED + audit_by/at/score |
| t12 | 统计/导出 | rpt_question_stat 累加; exp_task 产出 .sav/.xlsx |

### 4.7 异常分支全枚举与处置

| 异常 | 检测 | 处置 | 数据后果 |
| --- | --- | --- | --- |
| 无人接听 | 振铃超时(30s) | 结果码 NA；按重拨规则回池 | attempts+1；不刷新"接通时间" |
| 占线/语音信箱/传真音 | 信令/音检测 | BUSY/VM/FAX；传真可自动标 valid_flag=0 | smp_phone.valid_flag=0 |
| 空号 | 运营商回码 | INVALID；自动入黑名单（码表配置） | hit_black_flag 生效 |
| 接通无坐席（呼损） | 预测算法兜底 | 致歉挂断；abandon+1；样本回池优先重拨 | 纳入呼损率监控与算法反馈 |
| 坐席端断线（通话中） | 心跳 | 话务保持/可转接；答卷 DOING 保留 | 重连续答；超时样本回收 |
| 拒访/中途挂断 | 坐席提交 | REFUSE（入黑名单可配）/BREAKOFF（可重拨） | 闭环或回池 |
| 不符合甄别 | 甄别题跳 END | QUFAIL 闭环；不入成功样本 | 配额不计 |
| 预约后未回拨 | 到期扫描 | 提醒→EXPIRED | 预约转化率统计 |
| 审核驳回 | 督导动作 | 生成重访（新号码或同号） | REJECTED→新 sheet 关联 |
| 录音丢失/损坏 | 对账任务 | 标记缺失；审核页警示 | 质检留痕 |
| DB 故障中断作答 | 事务 | 每题独立提交，最大 loss=1 题 | 幂等 upsert 重放 |
| 时段窗外运行 | 策略闸门 | 自动挂起外呼，呼入不受影响 | 项目保持 RUNNING |

### 4.8 容量与性能参考

- 坐席并发：ITACATI 参考值 **每 WebServer 支持约 50 坐席**【S】；NK3C 按云上横向扩容【S】；
- 派样 QPS：100 坐席 × 1 通/3 分钟 ≈ 0.6 通/s，峰值(整点开班)按 10 倍设计；
- 录音存储：G.711 64kbps ≈ 28 MB/小时/路；100 坐席 × 4h/日 ≈ 11 GB/日，1 年 ≈ 2.7 TB（预留压缩与归档策略）；
- 答卷表：单项目 5 万样本 × 30 题 = 150 万行 ans_answer，按 sheet_id 分区/归档；
- 报表：实时数（监控/配额）走 Redis，落表聚合（rpt_*）T+1s~T+1min；历史大表不直接跨表 join 实时查询；
- JVM/部署：每程序独立 Tomcat，-Xmx768m 起步，机器 2G+【S】。

---

## 5. 菜单级验收清单（DoD 摘要）

- [ ] M02：四级组织建到"组"；四类权限码登录后菜单按 3.5 矩阵显隐；越权访问被控制器拒绝并留痕。
- [ ] M03：向导五步缺一不可启动；状态机非法跳转被拒；分发项目子库不可改题。
- [ ] M04：原型所列 20 题型中首批 ≥10 种可设计；校验能查出死题；发布强制校验；中途调整后新旧答卷版本并存可分。
- [ ] M05：导入 1 万条 <60s；黑名单/半年原则在派样 SQL 生效（对照 SQL 附录断言）；一人多号按序呼。
- [ ] M06：四种模式可配；时段窗外自动停拨；呼损率超限自动降速（压测验证 4.5.1）。
- [ ] M07：双人并发派样无重复（SKIP LOCKED）；断网 120s 内重连续答不丢题。
- [ ] M08：监听/插话/四类强控全部落 cti_monitor_event；配额 95% 触发报警。
- [ ] M09：审核页音题联动；驳回自动生成重访任务。
- [ ] M10：单题统计 T+1s 内刷新（实时口径）；交叉表与 SPSS 导出值一致。
- [ ] M11：SPSS 导出可用变量标签/值标签被 SPSS 正确读取；导出有审批与审计。
- [ ] M12：致命项一票否决生效；申诉流闭环。
- [ ] M13：自定义字段+布局保存后坐席端即时生效；SLA 超时升级触发。
- [ ] M14：两级以上菜单+转人工+留言转工单全通。
- [ ] M15：半年原则等参数改后 5 分钟内全局生效。

---

## 6. 配套文件对照索引

| 本文章节 | 对应文件位置 |
| --- | --- |
| 第 2 章 M04.2 问卷设计器 | `ITACATI_问卷设计器_交互原型.html`（题型库/画布/属性/校验/预览/配额/导入导出/打印） |
| 第 2 章 M07 执行管理（坐席面） | `ITACATI_坐席端_外呼作答原型.html`（签入/软电话/派样/呼出模拟/作答/结果码/预约/督导事件） |
| 第 2 章 M08 实时监控 | `ITACATI_督导端_监控墙原型.html`（坐席卡墙/监听/插话/强制操作/配额进度/报警，内置仿真） |
| 第 2 章 M14 IVR 管理 | `ITACATI_IVR设计器_交互原型.html`（节点树/按键分支/查询节点/来电模拟器） |
| 第 2 章各表"落表"列 | `ITACATI_NK3C_数据库建表.sql`（01~13 节建表；Oracle/SQLServer 版为迁移稿） |
| 第 3.1 菜单编号 | SQL 第 15 节 `sys_resource` 初始化（101~115 与 1021~1155） |
| 第 3.5 角色权限 | SQL 角色/授权初始化 + `sys_role_resource`；用例集"01 用例明细"权限类 |
| 第 4.2.3 样本状态机 | SQL `smp_sample.status` CHECK/注释 + `smp_status_code` 码表；坐席端原型结果码逻辑同源 |
| 第 4.5.1 预测拨号 | `autotest/test_logic_algorithms.py::TestPredictiveDial`（含降速/提速/时段外用例） |
| 第 4.5.2 派样算法 | SQL 附录样例 1 + `autotest` TestSampleFilter + test_db_dispatch.py（SKIP LOCKED） |
| 第 4.5.3 配额扣减 | SQL 附录样例 2 + `autotest` TestQuotaAtomic（1000 并发恰 100） + test_db_dispatch.py |
| 第 4.6 数据轨迹 | SQL 06/07 节表 + 视图；用例集 E2E-001 逐步断言 |
| 第 5 章验收清单 | `ITACATI_NK3C_测试用例集.xlsx`（00 概览 / 01 用例明细 / 02 P0 冒烟清单）+ `autotest/` 三层执行 |
| 背景与公开依据 | 《南康科技_ITACATI_NK3C_开发文档.md》第 1~17 章（含 S1~S13 出处） |

> 完成日期：2026-09-17。本套文件（主文档 + 本分析 + 三版 SQL + 两份交互原型 + 测试用例集）构成可据以开发的完整闭环；与南康实际实现有差异之处均以【推定】标注。
