# ITACATI NK-3C 项目交接文档

> 交接日期：2026-09-18 · 仓库：https://github.com/twm711/wkcati · 分支：`main`
> 本文面向**接手本项目的开发者/团队**：一小时内在本机跑起全部三个服务并理解全貌。

---

## 0. 一页速览

| 项 | 值 |
| --- | --- |
| 项目定位 | 南康科技 ITACATI/NK-3C 电话外呼调查系统（CATI）的复刻交付 + 现代化重构 |
| 双轨策略 | `nk3c-demo`（Python/FastAPI）= **行为基准**，`nk3c-go`（Go+React）= **目标栈**，断言逐条对齐 |
| 当前进度 | 文档/原型/建表/接口/用例全交付；demo 31 用例全绿；Go M0–M3 核心能力落地（-race 全绿）；React 六页控制台（tsc）；**M2 话务域收官：呼入 IVR + 外呼自动调研 + B2BUA 坐席桥接 + 录音落盘/回放 + flow 热更 SIP 验证（SIP E2E ×5 全绿 + gophone 演练 6/6）；M3 导出/WS/QC 基础能力已完成，M5/M6 生产化未完成** |
| 下一步 | M2 剩余（外呼腿 B2BUA/录音落盘）→ 导出/WS 移植 → 多机拓扑 |
| 环境红线 | **2GB 内存下两处 OOM**（modernc/sqlite 编译、vite build）——生产构建 ≥4GB（§7） |

---

## 1. 项目背景与调研结论

- 业务来源：南康科技 ITACATI/NK-3C 公开资料与产品页调研（结论编号 S1–S13，均已固化进主文档与实现文档，引用处标【S#】）：
  - S1/S2：15 项功能清单、B/S/S 架构、精简/标准/企业版、外呼四分类、CTI Proxy 平台无关、独立样本池/消息中心、答卷审核+录音回放质检；
  - S3/S4：呼出策略四模式、分权分域、20 题型、半年原则/黑名单/打散样本规则、导出 Excel/Quantum/SPSS/Txt；
  - S5：IVR+留言+转人工、工单模板、实时质检六指令、962269 百席案例；
  - S6–S9：南康 nweb（Java 系）工程规范——ResultInfo 恒 200 信封、ID 18 位 Long→String、权限四 Code、分层规范；
  - S10–S13：公司沿革、高校 CATI 案例、前端信封处理教训。
- 关键规格锚点：OpenAPI 47 端点（`ITACATI_NK3C_API接口规格.yaml`）、60 表三版建表 SQL、96 条测试用例、P0 覆盖矩阵 ✅20/◐7/◻4/▢10。

## 2. 双轨技术栈

| | nk3c-demo（基准） | nk3c-go（目标栈） |
| --- | --- | --- |
| 后端 | Python 3 / FastAPI / SQLite | Go 1.23 / gin / database/sql |
| SQLite 驱动 | 内置 | **mattn/go-sqlite3（CGO）**；modernc 为 CGO-free 备选（编译 ≥4GB） |
| 前端 | 原生 HTML/JS（static/） | React 18 + TS strict + Antd 5 + Vite 5 + React Router 6（Hash） |
| 数据库 | SQLite（dev） | SQLite（dev）/ MySQL 8（driver=mysql 已接入迁移选择；SKIP LOCKED 与真实压测待完成） |
| SIP（规划） | — | **diago**（emiago，基于 sipgo）：Playback/录音/转接/B2BUA；gophone 做 E2E 测试 |

## 3. 仓库结构

```
├─ README.md / 交接文档.md / .gitignore
├─ 南康科技_ITACATI_NK3C_开发文档.md          # 主文档：调研→需求→设计→库表→接口→测试→部署
├─ 南康_ITACATI_NK3C_实现文档_GoReact版.md    # 目标栈实现文档（12 章+3 附录，M0–M6 37 人日）
├─ 南康_ITACATI_NK3C_部署手册.md              # 生产拓扑/硬件/JVM/容量/检查单
├─ ITACATI_NK3C_API接口规格.yaml + 接口说明.md # OpenAPI 3.0（47 端点）+ 人读版
├─ ITACATI_NK3C_数据库建表.sql(-Oracle/-SQLServer) # MySQL 基准 60 表 + 两移植版
├─ ITACATI_问卷设计器_交互原型.html 等 4 个    # 问卷/坐席/督导/IVR 评审原型
├─ ITACATI_NK3C_测试用例集.xlsx               # 96 条用例
├─ 南康_ITACATI_P0用例覆盖矩阵.md            # 41 条 P0 ↔ 断言映射（✅20/◐7/◻4/▢10）
├─ ITACATI_功能细化与全链路深度分析.md         # 功能细化分析
├─ index.html                                 # 交付导航页（文档/原型/Live 入口卡）
├─ autotest/                                  # pytest 自动化骨架（db/算法/原型冒烟）
├─ nk3c-demo/                                 # 🐍 行为基准
│  ├─ app.py（单文件后端，31 用例基线）  test_api.py
│  ├─ static/（四控制台 Live 页）  requirements.txt
└─ nk3c-go/                                   # 🚀 目标栈
   ├─ cmd/nk3c-api/main.go                    # 入口（--addr/--dsn/--reset）
   ├─ internal/{store,auth,project,agent,monitor,workorder,ivr,app}/
   ├─ internal/store/migrations/sqlite/001_init.sql   # 18 表+种子（与 demo 对齐）
   ├─ internal/itests/chain_test.go           # 9 链路集成测试（58 断言点）
   ├─ pkg/{rinfo,ids}/                        # ResultInfo 信封 / 雪花 ID→string
   └─ web/                                    # ⚛️ React 五页骨架（web/README.md）
```

## 4. 环境与本地运行

依赖：Go ≥1.23（CGO 开启）、Node ≥20 + pnpm、Python ≥3.11、gcc（CGO）。

```bash
# ① 行为基准（:8000）
cd nk3c-demo && pip install -r requirements.txt
uvicorn app:app --host 0.0.0.0 --port 8000
# 测试：python3 -m pytest test_api.py -v   （31 条全绿为验收基线）

# ② Go 后端（:8080 API + :5060 SIP 话务域）
cd nk3c-go && go build ./... && go vet ./...
go run ./cmd/nk3c-api --addr :8080 --sip-addr 0.0.0.0:5060 --reset   # --sip-addr "" 禁用话务域
# 测试：go test ./internal/itests/ -race -v        # 9 条业务链路
#       go test ./internal/media/ -v               # SIP/RTP 呼入 E2E（diago 回环，无需真机）

# ③ React 前端（:5173，/api 反代 :8080）
cd nk3c-go/web && pnpm install && pnpm dev
# 类型检查：pnpm build（tsc --noEmit + 产物构建；2GB 环境见 §7 OOM 说明）
```

**账号**（密码统一 `123456`）：`admin`=系统管理员（domainAdmin，可 `/api/sys/reset`）；`sup01`=督导（groupAdmin：质检审核/归档/项目管理）；`agent01`=坐席（工号 1020，外呼作答/工单受理）。

**种子场景**（与 demo 对齐，便于复现测试链）：项目 1 满意度（样本 101–106，仅 106 之外的 101 可首派：102 已闭环/103 黑名单/104 半年内已接通/105 超重拨上限）；项目 2 偏好调查；IVR 默认流程（1→调研、2→留言、0→转人工落工单）。

## 5. 测试与质量基线

| 基线 | 命令 | 规模 | 状态 |
| --- | --- | --- | --- |
| demo 行为断言 | `pytest test_api.py` | 31 条链路 | ✅ 全绿 |
| Go 集成 | `go test ./internal/itests/ -race` | 9 链路 / 58 断言点 | ✅ 全绿（含 race） |
| Go 静态 | `go build` / `go vet` | 9 包 + main | ✅ |
| 前端类型 | `pnpm build`（tsc strict） | ~2000 行 TSX | ✅（构建产物 OOM 见 §7） |
| 代理链 E2E | 登录→派样→作答×2→SUCCESS→CLOSED_SUCCESS+CONSUMED（经 ：5173 代理） | — | ✅ |
| SIP/RTP E2E | `go test ./internal/media/`：①呼入转人工落工单 ②外呼自动调研（派样→外呼→DTMF 答题→SUCCESS+配额+录音）③呼入录音落盘 | 3 条端到端 | ✅ |
| gophone 真机演练 | `bash autotest/sip_drill.sh`：真机 SIP 呼入→DTMF→工单/录音/回放 6 断言 | 6/6 | ✅ |
| B2BUA 桥接 E2E | `TestBridgeAgentCallE2E`：客户腿+坐席腿双人通话→接通联动→通话中实时作答→人工结果码闭环 | 1 条 | ✅ |
| flow 热更 E2E | `TestFlowHotSwapSIP`：PUT 流程→真实呼入按新流程走线 | 1 条 | ✅ |
| P0 矩阵 | 《南康_ITACATI_P0用例覆盖矩阵.md》 | ✅20/◐7/◻4/▢10 | demo 口径 |

Go 九链路：①登录鉴权/角色 ②派样四过滤+配额扣减+单题统计 ③结果码幂等 ④过滤排空/数值越界/NA 回池重拨 ⑤审核状态机 ⑥跨项目作答守卫+项目2 独立配额 ⑦项目全生命周期（建→题→配额→发布→导样查重→修订 v1.0→v1.1→暂停/恢复/结项）⑧工单回访闭环 E2E（IVR 转人工→落单→办结→归档→回访样本→外呼→审核→双链路关联）⑨监控墙+IVR 全走线（无效键/留言/结局）。

## 6. 关键设计决策记录（ADR）

1. **ResultInfo 恒 HTTP 200 信封**（承 S6/S13）：`{success,code,message,data}`；业务码 0/4001 参数/4010 未登录/4031 状态/4032 权限/4041 不存在/4091 冲突/4092 重复/5000。前端 axios 拦截器判**响应体 code**（4010→清会话回登录），绝不判 HTTP status。
2. **ID 一律 JSON string**：雪花 ID / 自增 Long 序列化为字符串，规避 JS 精度丢失（`pkg/ids`）。
3. **双方言存储层**：dev SQLite `SetMaxOpenConns(1)` 串行化写；prod MySQL 放开连接 + `SKIP LOCKED`/`ON DUPLICATE` 分支（M3 启用）。迁移为内嵌 SQL + `schema_migrations` 表；当前使用 Goose 兼容格式的自研迁移器，不是 goose CLI。SQLite/MySQL 已按 driver 选择目录，真实 MySQL 压测仍待完成。
4. **errAbort 哨兵=「提交」**：事务助手遇 `store.ErrAbort` 走 Commit（业务分支已写响应+数据），其他错误 Rollback。⚠️ 曾经踩坑：语义为“回滚”时派样话务记录被静默回滚（本轮已修并加测试）。
5. **单连接池防自锁**：SQLite `MaxOpenConns(1)` 下，前一批 rows 未 Close 就发下一条 Query 会“自己等自己”——所有 handler 采用「先收集、后补查」模式（本轮修了 3 处）。新代码必须遵守。
6. **SQLite 驱动取 mattn（CGO）**：modernc/sqlite 纯 Go 但其 `sqlite/lib` 单包编译峰值 ≥2.5GB 内存（2GB 沙箱 OOM）；mattn 峰值 ~600MB。逻辑名 `sqlite` 内部映射 `sqlite3`。
7. **前端 HashRouter + antd Layout 手搭**（不用 ProLayout）：适配任意反代/离线环境、减依赖；axios 信封直通拦截器。
8. **nk3c-demo 不删**：它是行为规格的活文档，Go 改动以 demo 断言为对齐基准。

## 7. 已知问题与约束（红线）

| # | 问题 | 影响 | 对策 |
| --- | --- | --- | --- |
| 1 | 2GB 内存环境 `vite build` OOM（antd 3100 模块，Rollup 峰值 >2GB） | 生产构建不可在小容器做 | ≥4GB 机器/CI 执行；或升级内存；验证口径 = tsc + dev E2E |
| 2 | 2GB 内存环境 `modernc.org/sqlite` 编译 OOM | 不可用 CGO-free 驱动（小内存时） | 已固定 mattn/go-sqlite3（需 gcc/CGO） |
| 3 | SQLite 单写者：MaxOpenConns(1) | 高并发写受限 | 生产切 MySQL（驱动已内置，M3） |
| 4 | Go 侧为 Goose 兼容格式的内嵌迁移器 | Down/回滚能力有限 | 后续评估正式 goose CLI；当前优先保证 SQLite/MySQL 双目录可运行 |
| 5 | IVR web 页为**只读视图+模拟器** | 设计器拖拽编排未实现 | 路线图 ④；PUT /api/ivr/flow 已通 |
| 6 | workorder CreateFromIVR 项目 ID 硬编码=1 | 多项目呼入归属不准 | M2 话务域一并解 |
| 7 | 沙箱重置后 Go 工具链/缓存丢失 | 需重装 Go+设 GOMODCACHE/GOCACHE | 见 §10「环境自举」 |
| 9 | diago v0.32.2/sipgo v1.6 需 **Go ≥1.23**（go.mod=1.23.0） | 老 go1.22 工具链编译失败 | 装 go 1.24.x（§10 已给 1.24.5 脚本） |
| 10 | 话务域 SIP/UDP 5060 + RTP 临时端口 | 容器/防火墙需放行 UDP | `--sip-addr` 自定义；纯 API 演示 `--sip-addr ""` 关闭 |
| 8 | demo/session 级认证夹具会被 FORCECHECKOUT 杀 | 自动化用例链中段需重登 | autotest 已按此写 |

## 8. 里程碑路线（M0–M6）

| 里程碑 | 内容 | 状态 |
| --- | --- | --- |
| M0 骨架 | gin+ResultInfo+RBAC+迁移+React 骨架 | ✅（差异记录于实现文档） |
| M1 业务闭环 | 派样引擎/配额原子/结果码去向/审核状态机/工单回访/IVR 解释器/五页 UI | ✅ 本仓库 |
| M2 话务域 | diago 接入（`media.IvrDriver` 接口隔离）、**呼入 IVR：真实 SIP/RTP DTMF→转人工落工单（E2E 全绿）** | ◐ 呼入已落地 |
| M3 生产化 | MySQL 真实连接压测、异步导出/质检任务、CTI 控制面 | ◐ |
| M4 质检增强 | 录音回放同步质检、实时六指令 | ◻ |
| M5 多机 | 话务域独立 nk3c-call、事件总线 | ◻ |
| M6 交付 | 性能/容量、验收、培训 | ◻ |

**近期待办（按优先级）**：① P0 矩阵按 Demo/Go/SIP/React 证据重映射 ② CTI 控制面（监听/插话/强挂/消息）③ DialerLoop 预测外呼 ④ IVR 可视化设计器 ⑤ 质检任务/评分/申诉 ⑥ MySQL SKIP LOCKED 压测与生产运维 ⑦ GitHub Actions。

## 9. 文档索引

| 要看什么 | 去哪 |
| --- | --- |
| 业务全景/需求/库表设计 | `南康科技_ITACATI_NK3C_开发文档.md` |
| Go+React 怎么做、为什么 | `南康_ITACATI_NK3C_实现文档_GoReact版.md`（含落地差异注记 ✅） |
| 接口契约 | `ITACATI_NK3C_API接口规格.yaml`（47 端点）+ `接口说明.md` |
| 库表 | `ITACATI_NK3C_数据库建表*.sql`；Go 侧现行 18 表：`nk3c-go/internal/store/migrations/sqlite/001_init.sql` |
| 怎么部署生产 | `南康_ITACATI_NK3C_部署手册.md` |
| 测试怎么算过 | §5 基线 + `nk3c-go/internal/itests/chain_test.go`（断言即规格） |
| 前端细节 | `nk3c-go/web/README.md` |
| 交互演示 | 各 `ITACATI_*_交互原型.html` + demo Live（:8000 四控制台） |

## 10. Git 与环境自举

- 远端：`https://github.com/twm711/wkcati`（main）。提交规范建议：`feat/fix/test/docs(scope): 摘要`；里程碑完成打 tag（`m1-done` 起）。
- 沙箱环境自举（如容器重置）：

```bash
# Go 工具链
curl -sL -o /tmp/go.tgz https://go.dev/dl/go1.24.5.linux-amd64.tar.gz && tar -C /usr/local -xzf /tmp/go.tgz
export PATH=$PATH:/usr/local/go/bin GOMODCACHE=/tmp/gomod GOCACHE=/var/tmp/gocache TMPDIR=/var/tmp
# pnpm
corepack enable pnpm --install-directory ~/.local/bin && export PATH=$PATH:~/.local/bin
# git（.git/config 不持久时）
git config user.name twm711 && git config user.email twm711@users.noreply.github.com
git remote add origin https://github.com/twm711/wkcati.git
```

- 推送：`git push origin main`（HTTPS 需 PAT；或改 SSH remote）。

## 11. 交接检查单（新接手人第一小时）

- [ ] 按 §4 起三个服务，浏览器验证 :8000 demo 控制台、:5173 web 登录 admin
- [ ] 跑 §5 三条测试命令，确认基线全绿
- [ ] 读 §6 八条决策记录（尤其 4/5 两条踩坑）
- [ ] 打开 `internal/itests/chain_test.go` 通读九链路——它就是现行行为规格
- [ ] 在 agent01 上手工走一遍：派样→作答→SUCCESS→（切 sup01）审核→（IVR 0 键）转人工→工单归档→回访样本
- [ ] 确认本机内存 ≥4GB（否则跳过 vite build，用 dev 模式）
- [ ] 有问题先查 §7 红线表，再查实现文档附录
