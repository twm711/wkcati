# 南康 ITACATI / NK3C 项目真实开发进度与 CATI 能力审查

> 审查日期：2026-09-20（Asia/Taipei）  
> 审查范围：仓库 HEAD `9c32179`、Go/React 实现、Python 行为基准、Go E2E、四份项目文档、OpenAPI/三版建表 SQL、P0 矩阵。  
> 结论性质：代码与测试证据优先；文档中的“已完成”只有在代码、路由或测试中能找到证据时才计入。

---

## 1. 结论先行

### 1.1 项目真实定位

当前不是“完整的生产 CATI 平台”，而是：

1. **Python Demo 行为基准**：业务规则与基础链路相对完整，31 条 pytest 用例作为规格基线；
2. **Go + React 可运行的核心闭环演示版**：已具备样本派发、问卷答题、结果码、审核、工单回访、基础 IVR、真实 SIP 回环、外呼自动调研、桥接、录音、导出和监控事件流；
3. **距生产 CATI 仍有明显距离**：预测外呼、坐席 SIP 生命周期、完整质检、问卷设计器、组织权限、报表、异步导出、审计合规、MySQL 真正切换、HA/监控/备份尚未完成。

### 1.2 总体成熟度判断

| 领域 | 代码真实状态 | 生产 CATI 判断 |
|---|---:|---|
| Python 行为基准 | 31 条 pytest 基线 | 可作为规则参考，不是生产系统 |
| Go 核心业务闭环 | 9 条链路，核心流程可跑 | 演示/联调级 |
| SIP/媒体 | diago 真实 SIP/RTP E2E，外呼/呼入/桥接存在 | 单机演练级，非生产话务平台 |
| React 控制台 | 6 个页面（含新导出中心），TypeScript 通过 | 可演示，管理能力不足 |
| 导出 | CSV/XLSX/SAV、列裁剪 | 同步导出级，不是任务中心 |
| 实时监控 | 监控墙 WS、QC 事件 WS、强签 | 事件可视化级，质检闭环不完整 |
| MySQL | 有独立 SQL 草案和部分方言分支 | 未完成真实切换/压测 |
| 运维生产化 | 基本未开始 | 不可直接作为生产交付 |

**综合判断：核心 CATI 业务链路约 45%～55%（以完整需求而非当前 Demo 范围计）；目标栈工程约达到“核心演示版/试验性 MVP”，尚未达到 5～100 席稳定生产系统。**

这个百分比不是代码行数比例，而是按 CATI 能力域、生产必要条件和原需求的功能广度估算。

---

## 2. 文档与代码的一致性审查

### 2.1 文档中可信且已有代码证据的部分

| 文档声明 | 代码证据 | 判断 |
|---|---|---|
| Python Demo 31 条测试全绿 | `nk3c-demo/test_api.py` 实际 31 个测试函数 | ✅可信 |
| Go 9 条核心业务链路 | `internal/itests/chain_test.go` 有 `Test01`～`Test09` | ✅可信 |
| 外呼自动调研 | `internal/media/outbound_e2e_test.go`、`internal/media/outbound.go` | ✅已实现，但属于自动调研腿 |
| 呼入 IVR、留言、转人工 | `internal/media/sip_e2e_test.go`、`internal/ivr/*` | ✅已实现基础链路 |
| B2BUA 坐席桥接 | `TestBridgeAgentCallE2E`、`internal/media/bridge.go` | ✅已实现 |
| 录音落盘与回放 | `audio_tap.go`、`GET /api/recording/:callId` | ✅已实现部分场景 |
| 监控墙 WS | `realtime/hub.go`、`TestMonitorWebSocket` | ✅已实现 |
| QC WS、强签 | `realtime/qc.go`、`TestQC_WsPush`、`TestQC_ForceCheckout` | ✅已实现 |
| CSV/XLSX/SAV | `internal/export/*`、4 个导出/WS 测试 | ✅已实现 |
| Web QC 事件表 | `web/src/pages/Monitor.tsx` | ✅已实现 |
| Web 导出中心 | `web/src/pages/Export.tsx`、`/exports` 路由 | ✅已实现 |
| CTI 扩展列 | SQLite `003_cti_extensions.sql` | ✅已实现，但迁移实现仍需加固 |

### 2.2 文档已经过时、表述过度或相互矛盾的部分

#### A. 顶层 README 没有同步到 M3

根目录 `README.md` 仍写：

- “Go + React M0+M1”；
- “M3+ 录音质检/SPSS 导出移植/WS Hub/多机拓扑”；
- Go 版本徽章为 1.22。

实际：

- `go.mod` 为 Go 1.23；当前 HEAD 已完成 M3-1～M3-5；
- 已有 SIP、录音、SAV、WS、QC WS、Web 导出中心；
- 多机拓扑、完整录音质检仍未完成。

**风险：新接手人员会把已实现功能当成未实现，也会误判版本和验收范围。**

#### B. 实现文档的 M3 编号与顺序错乱

实现文档第 393～394 行把 M3 第四增量写在第三增量之前。M3-3 Web QC 面板、M3-4 CTI 迁移、M3-5 导出中心/MySQL 实际提交顺序应为：

1. M3-1 导出基础 + 监控 WS；
2. M3-2 QC WS + 强签；
3. M3-3 Web QC 面板；
4. M3-4 CTI 扩展列迁移；
5. M3-5 Web 导出中心 + MySQL 方言草案。

#### C. “goose”名义与实际实现不一致

文档多处说“迁移为 goose”“M3 换 goose CLI”，但实际是：

- `store.go` 自定义读取 `embed.FS` 的迁移器；
- 自己维护 `schema_migrations`；
- 没有 goose CLI 依赖、命令或真实 goose migration 执行链；
- `-- +goose Up/Down` 只是兼容标记。

应称为“Goose 兼容格式的内嵌迁移子集”，不能称为已经接入 goose CLI。

#### D. MySQL“支持”需要区分“已接通路径”和“已验收”

当前新增了 `migrations/mysql/001_init.sql` 和 `003_cti_extensions.sql`，也对答题 upsert 增加了 MySQL 分支；但是：

- `store.Migrate()` 仍然只嵌入和读取 `migrations/sqlite/*.sql`；
- `migrations/mysql` 没有被 `embed`，也没有按 `Driver` 选择；
- 没有 MySQL 实例连接测试；
- 没有 MySQL 并发派样、事务隔离、`SKIP LOCKED` 压测；
- MySQL 迁移文件是演练草案，不等于生产切换完成。

因此应标为：**MySQL 运行时迁移路径已接通，但真实实例连接、SKIP LOCKED、并发压测和故障演练未完成。**

#### E. 47 个 OpenAPI 端点不是 Go 实现端点数

`ITACATI_NK3C_API接口说明.md` 和 YAML 是目标规格，包含组织、策略、质检任务、异步导出、交叉报表等端点；Go `app.go` 实际只注册了核心子集。当前 Go 路由可以确认存在的主要域是：

- auth/reset；
- project 生命周期、加题、配额、样本导入；
- agent dispatch/answer/result；
- monitor wall/calls；
- sheet/audit、single report；
- workorder；
- recording；
- ivr flow/call/logs；
- monitor WS、QC WS、强签、同步 export。

目标规格中的这些域在 Go 中没有对应实现：

- `/api/org/*` 组织 CRUD；
- `/api/strategy/*` 呼叫策略 CRUD；
- `/api/agent/login`、坐席状态、浏览器坐席 SIP 生命周期；
- `/api/monitor/listen`、`/force`、`/message`；
- `/api/report/cross`、traffic；
- `/api/export/task` 异步任务与下载；
- `/api/qc/task`、`/api/qc/result`；
- 问卷独立 CRUD/校验/发布全套端点；
- 系统参数管理。

因此必须把 YAML 明确标成“目标 API 规格”，另提供“Go 已实现 API 清单”。

#### F. P0 矩阵混用了 Demo 证据、原型证据和 Go 证据

P0 矩阵自己注明“demo 口径”，但部分说明会让读者误解为 Go 已完成。例如：

- “监听与插话 ✅已自动化”：当前 Go 没有 `listen`、`barge` 路由；
- “录音-答卷联动审核 ✅”：当前 Go 有录音回放和答卷审核，但没有看到播放器进度与逐题高亮联动实现；
- “成功电话 12 步轨迹 ✅”：主要是 Demo/itests 核心链路证据，不等于真实 SIP + 桥接 + 录音 + 审核全链路已覆盖；
- “断线续答不丢题 ✅”：Go 有 upsert，但前端刷新/恢复会话体验仍不是完整坐席断线恢复方案。

应把 P0 增加 `证据轨道` 列：Demo、Go API、Go SIP E2E、React UI、规划。

---

## 3. 当前 Go + React 的真实完成清单

### M0：工程骨架与基础设施

**已完成：**

- Gin、ResultInfo、Bearer/Query Token 鉴权；
- 基础 RBAC 角色判断；
- React + Antd + HashRouter + Axios 信封处理；
- SQLite 内嵌迁移和重置；
- 基础项目、坐席、监控、工单、IVR 页面。

**仍未完成：**

- OpenAPI 契约测试；
- CI/CD、制品、容器、生产配置管理；
- goose CLI；
- 统一审计日志、密钥轮换、限流、CSRF/Origin 策略；
- 完整组织/资源权限模型。

### M1：核心业务闭环

**已有：**

- 派样过滤：IDLE、半年原则、黑名单、重拨上限；
- 基础配额原子扣减；
- 题目实时 upsert；
- 结果码去向与幂等；
- 答卷审核 PASS/REJECT/VOID 基础状态；
- 项目生命周期、版本修订、样本文本导入；
- 工单 PENDING→ACCEPTED→RESOLVED→CLOSED 与回访样本；
- IVR 解释器基础走线。

**真实边界：**

- 迁移表只有约 19 张，不是主数据库 60 张，也不是文档称的 33 张完整 M1 表；
- ID 仍主要是 SQLite 自增整数，和规格“Long/18 位 ID 前端按 String”不一致；
- 导样是姓名/电话文本提交，不是 Excel/CSV 字段映射、批次、失败报告；
- 项目/问卷设计是简化表单，不是完整设计器；
- 结果码与回访有硬编码和演示假设。

### M2：话务域

**已确认：**

- diago/sipgo 呼入 SIP/RTP；
- DTMF telephone-event；
- IVR 播报、菜单、留言、转人工；
- 外呼自动调研；
- B2BUA 客户腿/坐席腿桥接；
- 基础 WAV 录音落盘和回放；
- flow 热更；
- gophone 真机脚本。

**未完成或不能按生产标准认定：**

1. **坐席 SIP 生命周期不完整**：没有看到坐席注册、心跳、设备状态、分机绑定、坐席软电话管理；桥接接口接收的是外部 `agentUri`，更像测试/演练入口。
2. **自动外呼不等于预测外呼**：没有 DialerLoop、任务队列、并发线路管理、拨号倍率实时调节、呼损上限控制、时段窗、主叫号码池。
3. **桥接录音未完成**：文档已经承认 proxyMedia 模式的录音 tap 未接入；当前录音能力不能覆盖所有真人桥接通话。
4. **没有生产级 RTP/SIP 运营能力**：重试、超时、媒体协商矩阵、NAT、注册失效、线路故障、SIP 码映射、录音留存均未形成完整闭环。
5. **呼入业务归属仍有演示硬编码**：`CreateFromIVR` 使用 `project_id=1`，多项目呼入路由不准确。

### M3：实时、导出、质检

**已有：**

- 监控墙 2 秒 WS，失败降级 5 秒轮询；
- QC WS：DIAL/ANSWER/RESULT/FORCE_LOGOUT；
- 事件落 `cti_monitor_event`；
- 督导强签：吊销会话、样本回池；
- CSV/XLSX/SAV 同步导出；
- Web 列选择、列裁剪、本地历史；
- CTI 扩展列迁移。

**尚未达到文档/行业要求：**

- 导出不是异步任务，没有进度、队列、审批、服务端历史、审计；
- 没有录音批量 ZIP + CSV 索引；
- SAV 尚未证明完整值标签、缺失值、变量字典和生产 SPSS 兼容范围；
- QC 只有事件流，不是质检任务/抽检/评分表/缺陷/申诉/报告；
- 没有 LISTEN/BARGE/STOP、强制挂断、强制示忙/示闲、督导消息等 CTI 控制面；
- CheckOrigin 当前直接 `return true`，生产安全策略不合格；
- QC token 放在 URL 中，可能进入代理/访问日志。

### M4：IVR + 工单

**已有：**

- flow JSON 解释器；
- 网页 IVR 模拟器；
- 真实 SIP 呼入 DTMF；
- 留言、转人工、工单闭环和回访样本。

**未完成：**

- React Flow/拖拽 IVR 设计器；
- 流程版本、发布、号码绑定、回滚；
- 音频上传、TTS、音频资产管理；
- QUERY 外部系统节点；
- 多项目/技能组/溢出/夜间路由；
- 留言管理页面与一键转工单完整 UI。

### M5：预测外呼与完整质检

目前基本未开始：

- 四种呼叫模式的生产策略引擎；
- 预测拨号并发循环；
- 呼损反馈控制；
- 时段、节假日、号码池、主叫号码；
- 抽检任务、评分模板、质检分、致命项一票否决、申诉；
- 压测和 64 路以上话务验收。

### M6：生产化

基本未开始：

- MySQL 真实连接和迁移；
- `FOR UPDATE SKIP LOCKED` 生产派样；
- Docker Compose/Kubernetes 拓扑；
- Redis/NATS 事件总线；
- metrics、日志聚合、告警、健康检查扩展；
- 备份恢复、录音归档、保留策略；
- 5 席灰度、容量和故障演练。

---

## 4. CATI 行业能力审查

### 4.1 样本与拨号前管理

| CATI 能力 | 当前状态 | 结论 |
|---|---|---|
| 样本池/多号码 | 基础 `smp_sample`、`smp_phone` | ✅核心有，缺接触历史/批次 |
| 半年原则 | 参数化过滤 | ✅有 |
| 黑名单 | 全局黑名单和结果码命中 | ✅基础有，缺项目级维护 UI |
| 万级导入 | 文本框逐行导入 | ❌ |
| 自定义字段/字典 | 未实现 | ❌ |
| 分层/系统/两阶段抽样 | 未实现 | ❌ |
| 坐席组公平分配 | 未实现 | ❌ |
| 预约回拨队列 | 只有部分结果去向/回访样本 | ❌完整能力 |

### 4.2 问卷与访谈执行

| CATI 能力 | 当前状态 | 结论 |
|---|---|---|
| single/number/text | Go 有基础支持 | ✅ |
| 20 种行业题型 | 未实现 | ❌ |
| 条件跳题/分支 | 种子流程基本线性，问卷分支未实现 | ❌ |
| 必答校验 | 数据字段存在，完整前端/后端阻断不足 | ⚠️ |
| 断线续答 | upsert 支持 | ⚠️ 数据层有，产品体验不足 |
| 版本化 | 基础 v1.0/v1.1 | ✅基础 |
| 发布前逻辑校验/BFS 死题 | 未实现完整问卷校验 | ❌ |
| 录音与题目时间轴联动 | 文档声称，Go React 当前未证实 | ❌按生产口径 |

### 4.3 CTI/话务能力

| CATI 能力 | 当前状态 | 结论 |
|---|---|---|
| SIP 呼入 | diago 回环/真机演练 | ✅演练级 |
| SIP 外呼 | 自动调研外呼腿 | ✅ |
| 浏览器软电话 | 无 | ❌ |
| 坐席 SIP 注册/状态 | 无完整实现 | ❌ |
| B2BUA 桥接 | 有 | ✅演练级 |
| 预测拨号 | 无 | ❌ |
| 渐进/预览拨号 | 部分手动/自动演示 | ⚠️ |
| 呼损降速算法 | autotest 逻辑基准有，未接入 Go DialerLoop | ⚠️ |
| 录音 | 呼入/自动外呼部分有；桥接 tap 未完成 | ⚠️ |
| DTMF/IVR | 有 | ✅ |
| 监听/插话 | Go API 未实现 | ❌ |
| 强制挂断 | Go API 未实现；只有强签 | ❌ |
| 坐席消息 | 未实现 | ❌ |
| 号码池/主叫显号 | 未实现 | ❌ |
| 时段/节假日禁呼 | 未实现 | ❌ |

### 4.4 监控、质检与管理

| CATI 能力 | 当前状态 | 结论 |
|---|---|---|
| 在线墙 | WS + 轮询 | ✅ |
| DIAL/ANSWER/RESULT 事件 | QC WS | ✅ |
| 督导强签 | 有 | ✅ |
| 抽检 | 无 | ❌ |
| 评分表/致命项 | 无 | ❌ |
| 质检申诉 | 无 | ❌ |
| 坐席绩效 | 基础墙面统计，非完整报表 | ⚠️ |
| 实时报警 | UI/规则未实现 | ❌ |
| 组织/组/技能权限 | 基础角色字符串，不是完整组织模型 | ❌ |
| 审计日志 | 未实现统一 `sys_op_log` | ❌ |

### 4.5 报表、导出与合规

| CATI 能力 | 当前状态 | 结论 |
|---|---|---|
| 单题统计 | 有基础接口 | ✅ |
| 交叉统计 | 未实现 | ❌ |
| 话务/坐席/时段报表 | 监控墙聚合有限 | ❌完整报表 |
| Excel/CSV | CSV/XLSX | ✅ |
| SPSS | SAV 基础生成 | ⚠️需生产样本验证 |
| Quantum/TXT | 未实现明确格式 | ❌ |
| 异步导出任务 | 未实现 | ❌ |
| 导出审批/审计 | 未实现 | ❌ |
| 录音批量导出 | 未实现 | ❌ |
| PII 脱敏/访问审计 | 未实现 | ❌ |
| 录音保留/清理 | 未实现 | ❌ |

### 4.6 工单与呼入服务

| CATI 能力 | 当前状态 | 结论 |
|---|---|---|
| IVR 转人工 | 有 | ✅ |
| 工单状态机 | 有基础四状态 | ✅ |
| 自定义字段/布局 | 无 | ❌ |
| SLA/超时升级 | 无 | ❌ |
| 知识库 | 无 | ❌ |
| 多渠道统一工单 | 无 | ❌ |
| 回访联动 | 有演示闭环 | ⚠️项目 ID 硬编码，非泛化 |

---

## 5. 关键工程/安全问题

### P0：MySQL 路径并未真正打通

当前 `store.Migrate()` 固定读取 SQLite embed。即便 `Open("mysql", dsn)` 能建立连接，也不会自动使用新增 `migrations/mysql`；生产启动路径没有 `driver=mysql` 的完整参数、迁移、种子和健康验证。必须先解决再谈生产。

### P0：事件/迁移不是可靠生产事务模型

- QC 事件先写 `cti_monitor_event`，再广播，广播失败不影响落库，这是合理方向；
- 但迁移器逐条执行 SQL，之后才写版本，缺少真正迁移事务；
- `003` 为兼容旧库而吞掉 duplicate column，属于演示兼容策略，需改为 schema introspection 或正式 goose migration。

### P0：CTI 控制面缺失

只有强签，不足以支撑 CATI 督导。监听、插话、强挂、消息、强制状态切换属于行业基本能力，目标 API 中有声明，但 Go 路由没有落地。

### P1：数据模型远小于目标模型

目标建表 SQL 60 表，Go SQLite 迁移约 19 表。缺少组织、资源、策略、排班、预约、审计、报表、质检、录音、导出任务等表，很多功能不可能只靠当前核心表补齐。

### P1：前端完成的是“可操作演示”，不是管理工作台

页面已经能展示和调用核心接口，但：

- 项目导入是文本框，不是文件导入/映射/报告；
- IVR 是只读流程 + 模拟器，不是设计器；
- Monitor 是事件表 + 强签，不是 CTI 督导控制台；
- Export 历史只存在浏览器，不是组织可审计的服务端任务中心。

### P1：真实 SIP 验收面窄

已有 5 个 SIP 相关 Go 测试函数（呼入、外呼、录音、桥接、热更），质量不错，但它们是同进程/回环或演练场景，不等于多坐席、多线路、网络抖动、注册失效、并发呼损的验收。

### P1：安全基线不足

- WebSocket `CheckOrigin` 全放行；
- token 通过 URL query 传递，可能泄露到日志；
- 没有操作审计、导出审批、PII 脱敏；
- 默认账号/密码固定且文档公开；
- 录音访问没有细粒度项目/坐席范围审计。

---

## 6. 建议的下一阶段顺序

### 阶段 A：先把“真实状态”钉死（1～2 天）

1. 更新根 README、交接文档、实现文档里程碑状态；
2. 从 Go 路由自动生成“已实现 API 清单”；
3. P0 矩阵增加证据轨道，拆开 Demo/Go/SIP/React；
4. 补一个 `go test`：新库迁移、旧库升级、MySQL SQL 文件静态检查；
5. 将 MySQL 迁移按 driver 正式接入，或明确标注“不支持运行时 MySQL”。

### 阶段 B：补 CATI 必需的 CTI 控制面（优先于继续做 UI）

1. 统一 Call/Leg/AgentSession 模型；
2. 实现 `LISTEN`、`BARGE`、`STOP`、`HANGUP`、`FORCE_BUSY`、`FORCE_READY`、`MESSAGE`；
3. 让事件、控制指令、SIP 实际动作、审计日志形成一条可追踪链；
4. 桥接录音 tap 与双声道录音；
5. 增加 SIP 注册、坐席设备、线路/主叫号码配置。

### 阶段 C：补预测外呼生产核心

1. DialerLoop + 任务队列；
2. PREVIEW / PROGRESSIVE / PREDICTIVE / AUTO 四模式；
3. 时段、节假日、重拨间隔、号码轮换；
4. 呼损率反馈与降速算法接入真实呼叫状态；
5. MySQL `SKIP LOCKED` 派样并发压测；
6. sipp/gophone 多路压测和故障注入。

### 阶段 D：补质检与合规

1. qc_template/qc_task/qc_result/appeal 表；
2. 抽检比例、指定坐席/项目抽检；
3. 评分表、致命项一票否决、申诉；
4. 录音-答卷时间轴联动；
5. 操作审计、导出审批、PII 脱敏、录音留存清理。

### 阶段 E：再做完整报表与生产运维

1. 交叉统计、话务统计、坐席绩效、配额报表；
2. 异步导出任务、服务端历史、录音 ZIP；
3. Docker/Compose、MySQL、Redis/NATS、metrics、日志、备份恢复；
4. 5 席灰度→20 席→100 席容量验收。

---

## 7. 建议重新定义的里程碑状态

| 里程碑 | 建议真实状态 |
|---|---|
| M0 工程骨架 | ✅ 核心完成；生产 CI/安全/部署未完成 |
| M1 核心业务闭环 | ✅ 演示闭环完成；数据模型和管理端不完整 |
| M2 SIP/基础话务 | ✅ 演练闭环完成；生产 CTI 能力未完成 |
| M3 导出/实时/QC | ◐ 基础能力完成；异步导出、质检任务、控制面未完成 |
| M4 IVR/工单 | ◐ 解释器和基础工单完成；设计器、路由、资产管理未完成 |
| M5 预测外呼/质检 | ▢ 尚未完成 |
| M6 MySQL/生产化 | ▢ 尚未完成；仅完成静态方言演练 |

**最终审查结论：**项目已经从“骨架”推进到“可运行的 CATI 核心链路演示版”，真实价值集中在样本—答卷—结果码—工单—基础 SIP/录音—导出—事件流这条链上；但文档目前把目标规格、Python Demo、Go API、SIP E2E、React UI 的完成状态混写，导致进度看起来高于真实生产能力。下一步不应继续堆叠页面，而应优先完成 MySQL 运行时切换、CTI 控制面、预测外呼、质检任务/评分、审计合规和可重复的多席位压力验收。
