# 南康科技 ITACATI（电访专家）/ NK3C（云联络专家）系统调研开发文档

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 南康科技 ITACATI / NK3C 语音调研系统调研与开发文档 |
| 整理日期 | 2026-09-17 |
| 调研对象 | 上海南康科技有限公司（南康网络）· 电访专家 ITACATI（CATI 系统）· 云联络专家 NK3C（云联络中心平台） |
| 文档用途 | 产品调研 + 二次开发/仿制开发参考文档 |
| 配套文件 | ① `index.html` 原型与文档中心 ② 四端交互原型：`ITACATI_问卷设计器_交互原型.html` / `ITACATI_坐席端_外呼作答原型.html` / `ITACATI_督导端_监控墙原型.html` / `ITACATI_IVR设计器_交互原型.html` ③ 数据库：`ITACATI_NK3C_数据库建表.sql`（MySQL 基准）+ Oracle 版 + SQL Server 版 ④ 接口：`ITACATI_NK3C_API接口规格.yaml`（OpenAPI 3.0，47 端点已校验）+ `ITACATI_NK3C_API接口说明.md` ⑤ 测试：`ITACATI_NK3C_测试用例集.xlsx`（96 条）+ `autotest/` 自动化骨架 ⑥ 评审版：`南康科技_ITACATI_NK3C_开发文档_合并版.docx` ⑦ 可运行演示系统：`nk3c-demo/`（FastAPI+SQLite，31 集成用例全通过（**多项目/多问卷**：项目管理台 Live + 版本化修订；**工单回访闭环 E2E**：归档→回访样本→外呼→审核→双链路关联；**SPSS .sav 真实导出**；41 条 P0 覆盖矩阵 ✅20/◐7 见《南康_ITACATI_P0用例覆盖矩阵.md》）；`/` 控制台首页（坐席/督导/IVR/**项目管理台**/文档 五入口）→ **/agent Live 坐席台**、**/supervisor Live 督导台**（监控墙 **WebSocket 实时推送**/话务流水/答卷审核/**导出中心 CSV·XLSX**）、**/ivr Live IVR 流程设计器**（可视化编排+话机模拟按键走线）与**实时质检**（S5 监听/插话/示忙/示闲/强挂/强签，WS 定向下发坐席台）与**录音回放同步质检**（S2），`POST /api/sys/reset` 一键重置）⑧ 发行包：`南康_ITACATI_NK3C_完整交付包.zip` |；⑨ 部署手册：《南康_ITACATI_NK3C_部署手册.md》（生产拓扑/硬件配置/JVM/容量估算/检查单）；⑩ P0 覆盖矩阵：《南康_ITACATI_P0用例覆盖矩阵.md》（41 条 P0 ↔ 31 条在线断言映射，✅20/◐7/◻4/▢10）；⑪ Go+React 版实现文档：《南康_ITACATI_NK3C_实现文档_GoReact版.md》（Go/gin/sqlx+goose、React+Antd5、SQLite开发/MySQL生产、diago 纯 Go SIP，里程碑 M0–M6） —— **进度（2026-09-18）：M0+M1 已落地 `nk3c-go/`（Go 1.22 + gin + mattn/sqlite），`go build` / `go vet` / `go test -race` 全绿，9 条链路集成测试与 nk3c-demo 行为对齐**；**前端骨架 `nk3c-go/web/`（React 18 + Antd5 + Vite）已落地：五页（登录/坐席台/监控墙+审核/项目/工单/IVR）+ tsc 全绿 + :5173 代理链 E2E（生产构建需 ≥4GB 内存）**
| 资料性质 | 全部来自公开渠道（厂商官网、CTI 论坛厂商方案、高校实验室介绍、厂商前员工技术博客），**未使用任何厂商内部机密文档** |

---

## 1. 文档说明（必读）

### 1.1 标注体系

本文档中每类信息都做了可信度标注：

| 标注 | 含义 |
| --- | --- |
| 【S1】~【S13】 | 来自公开资料的原文信息，文末"参考资料"章节可查原始出处 |
| 【推定】 | 公开资料未直接披露，依据 CATI/呼叫中心行业通行做法与已有公开信息合理重构，**开发复刻时可作为设计蓝本，但与南康实际实现可能存在差异** |

### 1.2 重要结论先行

1. **ITACATI ≠ NK3C，二者是两代产品**：
   - **ITACATI（电访专家）**：南康 2000 年启动研发、2002 年推出的国内第一款自主知识产权 CATI（计算机辅助电话调查）软件，早期基于 **Windows + .NET Framework 2.0**，B/S/S 三层结构，是"语音（电话）调研"的核心产品。【S1】【S2】【S10】
   - **NK3C（云联络专家）**：2017 年推出的**云联络中心（Cloud Contact Center）平台**，SaaS/云计算模式，把电访（CATI 外呼调研）、呼入客服、工单、IVR、在线渠道（微信/微博/短信/邮件/WebChat）整合到一个平台，可视为 ITACATI 能力的云端化、平台化升级。【S4】【S5】
2. 厂商对**菜单树、界面截图、数据库结构**等细节并未完整公开，本文件中这些部分以【推定】标注给出行业标准的完整设计，可直接作为开发文档骨架使用。
3. ITACATI 曾有"**精简版 / 标准版 / 企业版**"三个版本，并支持菜单式功能定制（按模块选购）【S1】；NK3C 支持公有云、私有云、混合云部署。【S4】

---

## 2. 公司与产品背景

### 2.1 公司概况【S10】

- **上海南康科技有限公司 / 上海南康网络技术有限公司**：十余年专注调查业务为主的数据采集工具研发。
- 定位：房地产市场信息管理、电话外呼领域国内领先；连续多年保持 CAI 类产品市场占有率领先（CATI 市场份额宣称 60% 以上）【S1】。
- 客户群：电信运营商、银行、政府机构、呼叫中心、市调公司（如零点研究）、咨询公司、高校（厦门大学、北师大珠海分校等）、社科院、卫生机构等 100+ 客户，5000+ 坐席分布全国十几个省份。【S1】【S10】【S11】【S12】

### 2.2 CAI 数据采集产品线全景【S3】【S10】

| 产品 | 英文名 | 类型 | 说明 |
| --- | --- | --- | --- |
| 电访专家 | ITACATI | CATI | 计算机辅助电话调查（本文主体） |
| 面访专家 | ITACAPI | CAPI | 计算机辅助面对面访问，支持图片/音频/视频/3D/虚拟购物 |
| 移动面访专家 | ITACAMI | 移动 CAPI | 移动终端面访 |
| 网络调查专家 | ITACAWI / ITACANI | CAWI | 在线调查，可嵌入 OA/网站/微信公众号，支持 H5 |
| 访谈专家 | NET-FGD | 在线焦点小组 | 网络座谈会/深访 |
| 云联络专家 | NK3C | Cloud Contact Center | 云联络中心平台（2017），整合以上采集渠道 + 呼叫中心 |
| 分析展示 | 数视化专家 / 空间统计专家 | BI | 多源数据接入、拖拽式工作表、仪表盘、大屏 |
| 舆情 | 网络舆情监测分析系统 | 舆情采集 | 全网 7×24 监测、语义分析、预警 |
| 呼叫中心 | NK Call Center System | 呼叫中心 | 2003 年面世，BPO 外包型呼叫中心 |

### 2.3 ITACATI 发展沿革与版本【S1】【S2】【S10】

- 2000 年开始研发；2002 年正式推出（国内第一款自主知识产权 CATI）；
- 2007 年前后推出 **3.0 版本**：强化后台管理端、可拆分功能模块、VIP 语音方案、Oracle 大客户数据库方案；
- 版本分级：**精简版 / 标准版 / 企业版** + 菜单式功能列表自定义选购；
- 荣誉：国家技术创新基金项目、国家高技术成果转化 A 类项目、上海市信息化专项资金项目、上海市徐汇区信息化发展资金项目。【S3】

### 2.4 NK3C 定义【S4】【S5】

- **NK3C = NK + 3C（Cloud Contact Center 云联络中心）**，基于 SaaS 模式和云计算的云调研平台；
- 融合大数据和小数据采集、数据统计分析、数据展示发布；
- 围绕业务部门建立**客户服务（呼入）+ 主动回访（外呼）**两大业务平台；
- 典型落地：上海 962269 房地产交易登记热线（100 坐席）、上海地产 400 热线、上海 962121 热线、各地不动产登记热线。

---

## 3. 总体架构

### 3.1 ITACATI 经典架构（3.x 时代）【S1】【S2】

**形态**：基于 Windows 平台的 **B/S/S（浏览器 / Web服务器 / 数据库服务器）** 结构，可运行于单机、局域网、互联网三种环境；访员客户机与管理台通过 IE 浏览器访问。

```
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│ 访员客户机│  │ 管理台PC │  │ 督导PC   │  │ 质检PC   │   ← IE 浏览器（展现层）
└────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘
     │  HTTP      │            │            │  耳机话机 ←→ 话音
┌────┴────────────┴────────────┴────────────┴────┐
│                Web 服务器（可多台，每台≈50坐席）      │  ← .NET / nkframework
│   项目管理 │ 问卷 │ 样本 │ 监控 │ 答卷 │ 统计 │ 系统  │    （业务实现层）
└───────┬──────────────────────────────┬─────────┘
        │                              │
┌───────┴────────┐            ┌────────┴─────────┐
│   CTI Server    │            │   数据库服务器      │
│ (CTI Proxy 智能 │            │ SQL Server 2005 / │
│  预拨号/话务路由)│            │   Oracle 9i/10g   │
└───────┬────────┘            └──────────────────┘
        │ CTI 指令                     ▲
┌───────┴────────┐            ┌────────┴─────────┐
│ 交换机/PBX 或    │            │   独立样本池       │ ← 缓冲接触电话，
│ 语音板卡/VoIP网关│            │  （样本缓冲服务）    │   保证高并发
└────────────────┘            └──────────────────┘
        ▲
┌───────┴────────┐
│  独立消息中心     │ ← 各子系统信息交通枢纽（监听/消息/录音指令）
└────────────────┘
```

**四个关键架构设计**【S2】：

1. **独立 CTI Proxy（平台无关层）**：业务端只发出抽象业务指令（外呼、挂断、转接、监听…），CTI Proxy 负责把指令翻译成具体媒体平台（交换机 / 语音板卡 / VoIP 网关）的平台指令。换 CTI 平台业务代码不变，甚至同一业务可同时跑在多种媒体平台上。
2. **独立消息中心**：各子系统的信息交通枢纽（坐席状态、短消息、监控事件），新增功能子系统非常容易。
3. **独立样本池**：专门缓冲和处理"接触电话"（待拨样本），避免客户端大量直连数据库，保证大容量坐席并发下的高效性与数据一致性。
4. **nkframework + SOA 分层**：基于 .NET Framework 2.0 自研框架，SOA 设计模式；分层为 **展现层 / 业务实现层 / DAO（数据访问层）**，DAO 独立使系统支持多种数据库。

### 3.2 NK3C 云架构【S4】【S5】【S6】【S8】

**形态**：纯 B/S 架构的云平台（SaaS），自上而下多层次体系结构（接入层 → 平台能力层 → 业务应用层 → 呈现层）【S5】，支持公有云 / 私有云 / 混合云。【S4】

**后端技术栈**【S6】【S9】：

| 项 | 内容 |
| --- | --- |
| 语言/平台 | Java（Web）+ .NET（坐席软电话 NKZXAgent，需 .NET Framework 4.0）【S7】 |
| 框架 | Spring + MyBatis + Maven + Shiro（权限） |
| 连接池 | Druid |
| 数据库 | Oracle、SQL Server、MySQL 三选一（驱动可配置） |
| 应用服务器 | Tomcat（正式环境建议**每个程序一个 Tomcat**，便于独立升级）【S8】 |
| 视图 | springMVC + velocity（.vm 模板） |

**工程分层（Maven 多模块）**【S6】：

```
NK3C（父工程）
├── nmodel             数据模型层
│   ├── vo             与数据库字段一一对应（单主键统一叫 ID）
│   ├── enums          枚举统一定义在 common 对应枚举类
│   └── voMapping      VO 映射（注意大小写：IvrStage 而非 IVRStage）
├── ndal               数据访问层（继承 BaseDAO；MyBatis 封装 getDbUtilBase()；
│                      JDBC 封装；自有配置 getSqlSessionManager()；
│                      事务 startManagedSession(false)，用完必须关闭）
├── nserviceprovider   业务逻辑层（业务实现 + 业务逻辑单元测试）
├── nserviceconsumer   服务消费层（服务接口聚合，为后期分布式做准备；
│                      新增方法须在 nweb 的 service.xml 注册 bean）
└── nweb               Web 层（springMVC 控制器 + velocity 视图 + 静态资源）
```

**运行程序（进程）清单**【S6】【S8】：

| 程序 | 职责 | 内存占用（参考） |
| --- | --- | --- |
| nweb | 管理端 + 坐席 Web 界面主应用 | 堆 250M / PermGen 50M |
| nagentstateserver | 坐席状态服务器（坐席签入签出、话务状态、监控事件分发） | 堆 370M（单 Tomcat 推荐 -Xmx768m -XX:MaxPermSize=160m） |
| （其他 n\* 服务，如 IVR/录音/报表服务）【推定】 | 按模块拆分部署 | 每程序独立 Tomcat |
| NKZXAgent（客户端） | 坐席软电话（.NET 4.0，reg.bat 注册，TestAgt.exe 测试签入）【S7】 | 客户端进程 |

**坐席软电话对接参数**（LoadConfig）【S7】：零配置服务器 URL（如 `http://<服务器>:9080/was`）、虚中心号、密码、工号段（如 1020~1030 内取号不重复）。

**前端技术栈**【S6】：IE10 以上（兼容 IE9）；requireJS（模块化：私有变量/初始化/事件/执行/返回/全局函数）；jQuery 2.0；南康自研 **nkui** 组件库（operamasks-ui 已弃用）；复杂高频更新页面用 vuejs；每个 .vm 文件对应同名 js 和 css。

**部署拓扑【推定】**（结合 S4/S5/S6/S8）：

```
 Internet/专线
     │
┌────┴─────────────────────────────────────────┐
│ 云平台（公有云/私有云/混合云）                    │
│  ├─ 接入层：语音网关(SIP/中继) 微信/短信/邮件/WebChat│
│  ├─ 能力层：CTI/排队路由 IVR 录音 预测拨号 样本池    │
│  │          nagentstateserver 消息中心           │
│  ├─ 业务层：nweb（项目管理/问卷/样本/工单/质检/报表）│
│  ├─ 数据层：Oracle/SQLServer/MySQL + 录音存储     │
│  └─ 呈现层：管理端Web 坐席端Web(iframe软电话条)     │
└──────────────────────────────────────────────┘
     │                        │
 分布式坐席（VPN/网页）     远程督导/质检（浏览器）
```

---

## 4. 运行环境与部署要求

### 4.1 ITACATI 经典版硬件配置【S1】

**配置表 1：小于 32 坐席（最低配置）**

| 机器 | 操作系统 | 处理器 | 硬盘 | 内存 | 其他 |
| --- | --- | --- | --- | --- | --- |
| DatabaseServer + WebServer（可同机） | Win2003 Server SP1 | 双核 2G | 80G | 2G | 安装 SQL Server 2005 或 Oracle 9i |
| CTIServer | Win2003 Server SP1 | P4 2.8G | 160G | 1G | 连接交换机/板卡 |
| 访员客户机 | WinXP Pro | P4 2G | 60G | 512M | IE6 + 耳机话机 |
| 管理台计算机 | WinXP Pro | P4 2.8G | 60G | 1G | IE6 |

**配置表 2：100 坐席（最低配置）**

| 机器 | 操作系统 | 处理器 | 硬盘 | 内存 | 其他 |
| --- | --- | --- | --- | --- | --- |
| DatabaseServer | Win2003 Server SP1 | 双核 3G×2 | 80G | 4G | SQL Server 2005 或 Oracle 9i |
| WebServer ×2 | Win2003 Server SP1 | 双核 3G | 80G | 2G | 每台支持 50 坐席 |
| CTIServer ×2 | Win2003 Server SP1 | P4 2.8G | 200G | 2G | 每台支持 50 坐席 |
| 访员客户机 | WinXP Pro | P4 2G | 60G | 512M | IE6 |
| 管理台计算机 | WinXP Pro | P4 2.8G | 60G | 1G | IE6 |

### 4.2 NK3C 运行配置【S8】

- 单个程序放一个 Tomcat：推荐 JVM `-Xmx768m -XX:MaxPermSize=160m`；机器最低内存 2G；
- nweb：堆 250M、PermGen 50M；nagentstateserver：堆 370M（起步）；
- 正式环境每程序独立 Tomcat，便于升级与故障隔离；
- 数据库连接：MyBatis + Druid，SQL Server 驱动版本在 POM 以 `<sqlserver.driver.version>4.0</sqlserver.driver.version>` 管理【S9】。

### 4.3 坐席端部署步骤【S7】

1. 安装 .NET Framework 4.0；
2. 运行 `reg.bat` 注册 NKZXAgent 软电话组件（确认注册成功）；
3. 用 `TestAgt.exe` 测试软电话签入：配置零配置服务器 URL（`http://<host>:9080/was`）、虚中心号、密码、从工号段取唯一工号；
4. 数据库连接配置 `db.properties`；
5. 浏览器打开坐席页面，签入后软电话条与页面联动。

---

## 5. 功能全景

### 5.1 ITACATI 十五大功能模块【S1】【S12】

| # | 模块 | 核心能力 |
| --- | --- | --- |
| 1 | 预览式呼出和智能预拨号 | 预览呼出 / 预测拨号 / 自动筛除空号忙音 |
| 2 | 项目管理 | 项目全生命周期：设计→试访→执行→控制→统计【S2】 |
| 3 | 自定义字段 | 样本/客户资料字段可自定义扩展 |
| 4 | 多种抽样方式 | 随机拨号、分段抽样、按规则随机生成数据等【S3】 |
| 5 | 样本及母体管理 | 导入、分级、分配、黑名单、半年原则、打散 |
| 6 | 问卷设计及管理 | 20 种题型、逻辑校验调试、题库、所见即所得预览【S3】 |
| 7 | 访问执行 | 坐席问卷作答、自动跳题、自动校验 |
| 8 | 项目监控 | 配额、成功/失败量、进度实时监控 |
| 9 | 工作站监控 | 监听、录音、发消息（督导→坐席） |
| 10 | 答卷审核 | 事后录音回放 + 答卷同步审核【S2】 |
| 11 | 答卷、话务统计 | 话务统计/样本统计/单题统计/交叉统计【S3】 |
| 12 | 答卷编码 | 开放题编码、机编+人编 |
| 13 | 自定义报表 | 自定义话费规则统计、图表输出【S3】 |
| 14 | 结果、项目信息导出 | Excel / SPSS / Quantum / Txt；账号、配额条件导出 |
| 15 | 系统维护 | 用户、权限、参数、日志、数据管理 |

### 5.2 系统特点汇总【S1】【S2】【S3】

灵活配置的业务适应性；多级业务管理体系；支持**父子项目**；以业务功能整合 CTI；丰富的呼出模式（**智能预拨 / 人工预览 / VIP / 混合**）；灵活的呼出/呼入策略；严谨的数据管理与维护；强大的问卷（呼出/呼入脚本）管理；便捷的数据统计分析；完整的质量监控；无限的系统整合、扩展能力；分权分域（横向逻辑分层、纵向业务领域子系统划分）【S3】；多层数据存储（标准项目 / 分发项目，上下级公司数据上传下发）【S2】。

---

## 6. 管理端（后台）功能与菜单结构

> 说明：南康未公开完整后台菜单树。本章 6.1~6.15 中"模块划分与功能点"均来自公开资料【S1】【S2】【S3】【S4】【S5】；"子菜单/界面要素"为按该功能点与南康"标准管理界面（参考项目管理：列表 + 新增/修改弹出窗口）"【S6】风格做出的【推定】设计，可直接用作开发蓝本。

### 6.0 主框架与工作台【推定】

- 顶部：系统名称、当前用户（显示角色：域管理员/组织管理员/组管理员/一线员工【S7】）、修改密码、退出；
- 左侧：按权限动态生成的功能菜单树（NK3C 的菜单/按钮级权限挂在"资源"上，资源分类型，权限按 Code 判断，如 domainAdmin/orgAdmin/groupAdmin/phoneAdmin）【S7】；
- 中部：iframe 或内部元素（#parse / load 两种加载方式【S6】）加载各功能页；
- 工作台首页：待办（试访任务、待审核答卷、待处理工单）、项目执行进度卡、坐席在线状态、今日话务汇总。

### 6.1 组织与权限管理（分权分域）

**功能**【S3】【S7】：同一公司不同项目共享样本资料；分公司独立管理业务、独立部署、数据逻辑分开；横向逻辑分层（域→组织→组→员工），纵向业务领域子系统划分。

**菜单树【推定】**

```
组织权限
├── 域管理（domainAdmin）        新增/编辑/停用域；域参数；数据隔离策略
├── 组织管理（orgAdmin）         组织树维护；组织间样本共享设置
├── 部门/组管理（groupAdmin）    组增删改；组与坐席组映射；访问员组管理
├── 员工管理                     员工档案、工号（从工号段取号【S7】）、状态
├── 角色与资源授权               资源树（菜单/按钮/权限类型）；按 Code 授权
└── 操作日志                     登录日志、操作审计、导出
```

**界面要素（列表 + 弹窗）【推定】**：树形组织面板（左）+ 成员列表（右，列：工号/姓名/角色/组/状态/最后登录）；新增修改为弹出窗口（参照项目管理界面规范【S6】）。

**最小功能集**：域/组织/组/员工四级 CRUD；角色-资源授权；按 Code 的按钮级权限校验；登录与操作日志。

### 6.2 项目管理

**功能**【S1】【S2】：以项目管理理论串起外呼/调查项目五大过程（启动定义 → 计划设计 → 执行 → 监控 → 收尾输出）；父子项目/分发项目；项目分派与排班及运行控制【S5】。

**菜单树【推定】**

```
项目管理
├── 项目列表          全部项目：编号/名称/类型(调查·营销·回访·呼入)/状态/
│                     起止时间/配额进度/成功率；筛选：状态·类型·日期·组织
├── 新建项目（向导）    ①基本信息（名称、类型、客户、起止时间、项目时间段【S3】）
│                     ②问卷选择/新建 ③样本选择与抽样 ④呼出策略 ⑤坐席组分配
│                     ⑥配额设置 ⑦审核与发布
├── 项目复制 / 模板    历史项目复制；项目模板
├── 分发管理           标准项目/分发项目；子项目分发到子数据库/本地库【S2】
├── 项目控制           启动/暂停/终止；试访模式切换；执行流程定义【S2】
├── 项目分派与排班      项目→坐席组分派；排班表；项目时间段【S3】【S5】
└── 项目归档           结项、归档、删除保护
```

**界面要素（项目列表页）【推定】**：工具栏（新建/复制/分发/启动/暂停/导出）；列表（多选）；行内快捷图标（配置、样本、问卷、监控、统计跳转）；详情页用页签：概览 / 问卷 / 样本 / 策略 / 坐席 / 配额 / 监控 / 统计。

**最小功能集**：项目 CRUD + 状态机（草稿→试访→执行→暂停→结束→归档）；问卷/样本/策略/坐席四要素绑定；进度与配额实时汇总。

### 6.3 问卷（脚本）设计管理

**功能**【S1】【S3】【S4】：呼出/呼入脚本；20 种题型（NK3C 明确列出：单选、多选、开放、问答、数字、数值分配、表格、排序、三维表格、有序多选、哑题、分配题、辅助计算等【S4】）；逻辑关系校验、自动调试；题库积累拖选使用；所见即所得预览；设计器/调试器可单机运行；问卷打印；doc/xml/sur 多格式导入导出；问卷结构可调整（**访问中途也可调整问卷**【S4】）。

**菜单树【推定】**

```
问卷管理
├── 问卷列表          名称/编号/状态/版本/题数/预估时长/所属项目；复制、版本对比
├── 问卷设计器        左：题型/题库面板；中：所见即所得画布；右：题目属性
├── 逻辑与配额        跳转逻辑、显示逻辑、选项关联/互斥/排斥、随机轮题倒序【S3】；
│                     单重/交叉配额【S2】
├── 问卷校验/调试      自动纠错【S3】、逻辑校验报告、模拟作答调试器
├── 题库管理          常用题库、自建题库、分类维护、拖选使用【S3】【S4】
├── 模板管理          问卷模板、页面风格自定义【S4】
└── 导入/导出/打印     doc/xml/sur 导入导出【S3】；问卷打印
```

**界面要素（设计器）【推定】**：三栏布局；画布中每题卡片显示 题号/题干/选项/逻辑标记；题目支持字体设置（整体/单题【S3】）；选项分类展示【S3】；报错提示文案可自定义【S3】。

**最小功能集**：至少实现 单选/多选/开放/数值/排序/矩阵(表格) 六类题型；跳转与显示逻辑；配额；预览；逻辑校验；导入导出（JSON 自有格式 + Excel 打印版）。

> 完整题型规格见第 9 章。

### 6.4 样本与母体管理

**功能**【S1】【S2】【S3】：样本导入与按条件筛选；按规则随机生成号码数据（随机拨号/ Mitofsky-Waksberg 两阶段等抽样在案例中使用【S10】）；样本分级并分配不同访问员组；客户详细资料自定义添加；字典表；黑名单；**半年原则**（半年内被访问过的样本不再访问）；样本打散；样本字段批量修改；自定义每个电话结束的状态；一人多号排序【S3】。

**菜单树【推定】**

```
样本管理
├── 样本池            全部样本列表；状态（未呼/成功/失败细分/预约/禁呼）
├── 样本导入          Excel/Txt/CSV 映射导入；查重；字段映射模板
├── 随机抽样生成      号段规则、区域规则、抽样算法（随机/系统/分层/两阶段）
├── 样本筛选与打散     条件筛选器；打散重排
├── 样本分配          项目分配；访问员组分配；分级配额分配
├── 字段与字典        自定义字段；字典表维护；批量修改
├── 黑名单/半年原则    全局/项目级黑名单；半年内禁呼规则
└── 样本状态码        自定义每个电话结束状态（成功类/失败类细分码表）
```

**界面要素【推定】**：高级筛选面板（字段 + 运算符 + 值，可存为方案）；列表支持列自定义；批量操作工具栏（分配/导出/打入黑名单/打散）；导入向导（上传→映射→校验→预览→执行→报告）。

**最小功能集**：Excel 导入；随机生成；条件筛选；黑名单；半年原则；样本状态码表；按组分派。

### 6.5 呼出策略与话务管理

**功能**【S3】【S4】：智能预测 / 预览 / 自动 / 混合外呼；自定义项目时间、支持排班表；一人多号呼叫排序；呼叫方式、工单模式、配额联动；为访问员单独设置主叫号码；智能预测拨号自动筛除空号、传真、占线、无人接听；呼出量根据历史接通率自动调整；"呼损率"可控【S4】；VIP 语音方案【S1】；灵活的呼出/呼入策略设定【S1】。

**菜单树【推定】**

```
呼叫策略
├── 外呼策略列表       策略名/类型(预测·预览·自动·混合)/绑定项目/状态
├── 预测拨号参数       呼损率目标、坐席空闲率阈值、拨号节奏、无效号码自动标记
├── 预览呼出参数       坐席看样本后手动发起；跳过规则
├── 重拨规则          未接通重拨次数/间隔/换号规则（一人多号排序）
├── 时段与排班         允许外呼时段、节假日、排班表联动
├── 主叫号码管理       项目主显号/坐席主显号设置【S3】
└── 呼入路由          呼入队列、技能组路由、IVR 入口绑定、转人工策略
```

**最小功能集**：预览呼出 + 自动(渐进)呼出两种；时段控制；重拨规则；主叫号码设置；无效结果码自动归类。

### 6.6 执行管理（访问执行）

**功能**【S1】：坐席执行电话访问；系统按策略派样；电脑控制跳题与校验；呼入业务可通过一套问卷 + 首题引导实现多业务分流【S2】。

**菜单树【推定】**

```
执行管理
├── 执行总览          在线坐席、当前项目话务、接通率、平均时长实时数字屏
├── 坐席状态          签入/示忙/示闲/小休/通话中/后处理；强制签出
├── 样本派发监控       样本池消耗、回收、重拨队列
└── 预约与回拨         预约样本列表、到时提醒、回拨队列
```

**最小功能集**：坐席签入签出；样本自动/手动派发；通话结果码提交；预约回拨。

### 6.7 实时监控（项目监控 + 工作站监控）

**功能**【S1】【S2】【S5】：监控项目执行数据（配额、成功量、失败量）；对话务员实时**监听**；NK3C 实时质检接口：**监听、插话、强制示忙、强制示闲、强制挂断、强制签出**【S5】；督导向坐席**发消息**【S1】。

**菜单树【推定】**

```
实时监控
├── 项目监控          配额进度条、成功/失败/接通率曲线、时段话务图
├── 坐席监控墙         坐席卡（状态/时长/当前项目/当前样本）；点击坐席：
│                     监听/插话/拦截/强制示忙/示闲/挂断/签出/发消息
├── 录音实时流         通话中录音监听【推定，与话路联动】
└── 报警提醒          配额将满、成功率异常、坐席长时间无通话
```

**最小功能集**：坐席状态墙；监听；发消息；配额与成功量实时统计；强制示忙/示闲。

### 6.8 答卷管理（审核 + 编码）

**功能**【S1】【S2】：答卷审核（可批量审核【S4】）；**录音回放与答卷同步审核**以确保真实性；指定录音另存/导出【S2】；开放题编码（机编 + 人工编码）【S1】【S3】；NK3C 支持答卷审核指标设置【S5】。

**菜单树【推定】**

```
答卷管理
├── 答卷列表          项目/坐席/样本/结果码/时长/审核状态；多条件筛选
├── 答卷审核          打开答卷：左录音播放（波形/变速）右问卷逐题比对；
│                     审核动作：通过/驳回/作废；批量审核；审核指标设置【S5】
├── 答卷编码          开放题答案列表→码表检索→赋码；编码进度与一致性复核
└── 录音管理          录音检索、试听、另存、导出、存储清理策略
```

**最小功能集**：答卷查询；录音回放与答卷联动；通过/驳回；开放题码表与赋码。

### 6.9 统计分析与报表

**功能**【S1】【S3】：话务统计、样本统计、单题统计、交叉统计；柱形图、饼状图（含三维）；统计结果实时查看（无需导出）；单项目、多项目数据统计；自定义话费规则并统计；自定义报表。

**菜单树【推定】**

```
统计报表
├── 话务统计          呼出量/接通率/平均通话时长/坐席效率/时段分布
├── 样本统计          样本消耗、状态分布、黑名单命中、预约转化
├── 单题统计          按题汇总频数与百分比（实时）
├── 交叉统计          任意两题（或多题）交叉表；与样本字段交叉
├── 配额统计          配额完成度实时表
├── 话费统计          自定义话费规则（费率/计费口径）→ 项目话费
├── 坐席绩效          话务量、成功率、质检成绩、考勤联动（绩效考核【S4】）
└── 自定义报表        字段拖选、图表类型（柱/饼/线，含三维）、定时导出
```

**最小功能集**：话务、样本、单题三类统计 + 图表实时展示 + Excel 导出。

### 6.10 数据导出

**功能**【S1】【S3】：按项目整体导出；**Excel / Quantum / SPSS** 格式导出；管理人员、话务员账号导出；项目配额条件导出；原始数据以 Txt、Excel 格式导出。

**菜单树【推定】**

```
数据导出
├── 答卷数据导出       格式：Excel / SPSS(.sav) / Quantum / Txt；
│                     范围：按项目/按结果码/按时间；变量字典（SPSS元数据）
├── 录音批量导出       按项目/坐席/时间批量打包
├── 账号导出          管理与话务员账号
├── 配额条件导出       配额定义与完成数据
└── 导出任务          异步任务队列、下载中心、权限审批【推定】
```

**最小功能集**：Excel/Txt 原始数据导出 + SPSS 格式导出（含变量标签）。

### 6.11 质检管理（NK3C 强化）【S5】

**功能**：话后录音质检、工单质检、答卷审核三合一；实时质检（监听/插话/强制操作）；质检评分表与考核。

**菜单树【推定】**

```
质检管理
├── 质检任务          抽样规则（比例/指定坐席/指定项目）自动生成任务
├── 录音质检          评分表打分（服务规范/业务准确/流程合规）、缺陷标记
├── 工单质检          工单内容合规性检查
├── 答卷质检          与答卷审核联动；审核指标设置【S5】
├── 评分表管理        评分模板 CRUD、权重
└── 质检报告          坐席质检成绩排名、问题类型分布、申诉流程
```

**最小功能集**：按比例抽样生成质检任务；录音回放打分；成绩统计。

### 6.12 工单管理（NK3C 呼入业务）【S5】

**功能**：自定义创建客服项目、项目分派、项目排班及运行控制；自定义工单字段、字段布局；工作流模板选择及节点设置；坐席据此处理业务咨询、投诉建议等。

**菜单树【推定】**

```
工单管理
├── 工单列表          工单号/类型/来源渠道/客户/状态/处理人/时限
├── 工单字段与布局     字段定义（文本/日期/字典/附件）；布局拖拽
├── 工作流模板        节点（受理→处理→回访→归档）、流转条件、超时升级
├── 工单分派          手动/规则自动分派；改派；催办
├── 知识库            业务知识条目维护，坐席端弹窗检索（呼入辅助【推定】）
└── 回访联动          工单结案自动生成回访外呼任务（与外呼项目联动【S5】）
```

**最小功能集**：工单 CRUD + 状态流 + 字段自定义 + 分派。

### 6.13 IVR 自助服务（NK3C）【S5】

**功能**：支持任意级数、任意回合跳转的 IVR 流程；支持语音留言；7×24 全天候并行处理多路来话；语音菜单（如办证结果查询、业务咨询、投诉建议）；自动业务不能满足时转人工。

**菜单树【推定】**

```
IVR管理
├── 流程列表          流程名/绑定呼入号码/版本/状态
├── 流程设计器        节点：播报/按键菜单/查询接口/留言/转人工/定时/条件分支
├── 语音文件管理      提示音上传、TTS 合成【推定】
├── 留言管理          留言列表、试听、转工单
└── 话务路由          时段路由、溢出转人工规则、排队策略
```

**最小功能集**：两级按键菜单 + 转人工 + 留言。

### 6.14 排班与绩效考核【S3】【S4】【S5】

排班表（项目时间段、坐席班次）、考勤（签入时长）、绩效（话务量/成功率/质检成绩）、坐席技能（方言等技能标签影响派样【S12 参考 CATI 通行能力】）。

### 6.15 系统维护【S1】

**菜单树【推定】**

```
系统维护
├── 参数配置          系统参数、话务参数、界面参数
├── 数据字典          全局码表（结果码、状态码、行业码）
├── 数据备份恢复      备份策略、录音归档、清理
├── 许可证管理        坐席数授权（产品按坐席授权）【推定】
├── 接口管理          对外 API、与业务系统（如不动产登记平台）对接【S5】
└── 日志与巡检        系统日志、异常告警
```

### 6.16 管理端完整菜单树汇总（一页图）【推定·蓝本】

```
管理端
├── 工作台
├── 组织权限（域/组织/组/员工/角色授权/日志）
├── 项目管理（列表/新建向导/分发/控制/排班/归档）
├── 问卷管理（列表/设计器/逻辑配额/校验调试/题库/模板/导入导出打印）
├── 样本管理（样本池/导入/随机抽样/筛选打散/分配/字段字典/黑名单半年原则/状态码）
├── 呼叫策略（外呼策略/预测参数/重拨规则/时段排班/主叫号码/呼入路由）
├── 执行管理（总览/坐席状态/派样监控/预约回拨）
├── 实时监控（项目监控/坐席监控墙/实时录音/报警）
├── 答卷管理（答卷列表/审核/编码/录音管理）
├── 统计报表（话务/样本/单题/交叉/配额/话费/绩效/自定义）
├── 数据导出（答卷/录音/账号/配额/任务）
├── 质检管理（任务/录音质检/工单质检/答卷质检/评分表/报告）
├── 工单管理（列表/字段布局/工作流/分派/知识库/回访联动）
├── IVR管理（流程/设计器/语音文件/留言/路由）
└── 系统维护（参数/字典/备份/许可证/接口/日志）
```

---

## 7. 坐席 / 访员端

### 7.1 登录与签入【S7】

浏览器打开坐席 URL → 输入工号密码 → 软电话 NKZXAgent 自动随启 → 选择虚中心/技能组签入 → 状态变为"示闲"进入排队派样。软电话组件需预先 reg.bat 注册并用 TestAgt.exe 验证。【S7】

### 7.2 软电话条（坐席端常驻）【S7】【S5】【推定界面】

| 按钮 | 功能 |
| --- | --- |
| 签入 / 签出 | 上线 / 下线（督导可强制签出【S5】） |
| 示忙 / 示闲 | 暂停派样 / 恢复（督导可强制【S5】） |
| 应答 / 挂断 | 通话控制（预测呼出自动接通；督导可强制挂断【S5】） |
| 保持 / 取回 | 通话保持 |
| 转接 / 会议 | 咨询转、 blind 转、三方【推定】 |
| 音量 / 静音 | 本地话音控制 |

状态区显示：工号、状态（通话/后处理/示忙…）、当前项目、通话计时。

### 7.3 外呼访问界面（ITACATI 访问页面）【S1】【S2】【推定布局】

```
┌───────────────────────────────────────────────┐
│ 顶部：项目名 | 样本编号 | 通话计时 | 结果码提交区    │
├──────────────┬────────────────────────────────┤
│ 左栏：样本信息  │  右栏：问卷区（当前题大字号显示）      │
│ ·姓名/性别/区域│  · 题干（支持字体设置）              │
│ ·电话1/2/3    │  · 选项列表（单选/多选，支持随机轮换）  │
│ ·自定义字段    │  · 开放题输入框（自动校验）           │
│ ·历史接触记录  │  · 逻辑提示（自动跳题，必答校验）       │
│ ·拨号/换号按钮 │  · 备注/预约控件                     │
├──────────────┴────────────────────────────────┤
│ 底部：督导消息弹窗 | 结果码（成功/无人接/忙/拒访…）    │
└───────────────────────────────────────────────┘
```

要点：预测拨号时系统自动接通并弹出样本+问卷【S2 自动呼叫】；电脑根据问卷逻辑、已答答案、样本情况自动控制提问（显示/隐藏/跳过/随机）【S3】；答案即时入库、实时统计【S3】。

### 7.4 呼入界面【S2】【S5】【推定】

来电弹屏（主叫号码匹配样本/客户档案）→ 按 IVR 或技能组路由 → 坐席接听 → 左侧客户资料 + 历史工单，右侧业务引导问卷（一套问卷首题分流多业务【S2】）→ 生成工单（自定义字段/布局【S5】）→ 结束后处理 → 可自动生成回访任务。

### 7.5 坐席端最小功能集

签入签出、状态切换（示忙/示闲）、自动/手动外呼、样本信息展示、问卷作答（跳题/校验/随机）、结果码提交、预约、消息接收、通话计时。

---

## 8. 核心业务流程

### 8.1 电话调查项目全流程【S2】【S3】

```
①立项定义 → ②抽样建池 → ③问卷设计 → ④试访调试 → ⑤坐席排班培训
     → ⑥正式执行(呼出+监控) → ⑦实时质控(监听/配额控制) → ⑧答卷审核编码
     → ⑨统计分析 → ⑩导出交付/结项归档
```

项目管理五大过程组（启动、计划、执行、监控、收尾）由系统串接【S2】。

### 8.2 智能预测拨号流程【S2】【S4】

```
样本池取号 → 过滤(黑名单/半年原则/时段) → 预测算法按坐席空闲率与历史接通率
计算拨号量 → 发起外呼 → 空号/传真/占线/无人接自动归类回收 → 接通后匹配空闲坐席
并弹屏(样本+问卷) → 通话录音 → 挂机后处理(结果码) → 样本回池或闭环
```

### 8.3 呼入服务流程【S2】【S5】

```
来电 → IVR菜单(查询/咨询/投诉) → 自助应答(对接业务系统查询，如不动产登记平台【S5】)
   → 转人工(排队/技能组路由) → 坐席应答弹屏 → 生成工单 → 工作流处理 → 回访闭环
```

### 8.4 质检流程【S2】【S5】

实时质检（监听/插话/强制操作）+ 事后质检（录音回放↔答卷比对审核、工单质检）→ 评分 → 绩效 → 申诉。

### 8.5 分发项目（父子项目）流程【S2】

省公司设母项目（统一问卷、统一设定）→ 分发到地市子数据库/子项目 → 各地执行 → 本地统计本地数据 + 省级汇总全省数据；母/子项目操作权限不同。

---

## 9. 问卷设计器规格

### 9.1 题型清单

**ITACATI（20 种题型设计）【S3】**，结合 NK3C 官方明确列举题型【S4】归并为以下规格表：

| # | 题型 | 说明 | 输入控制 |
| --- | --- | --- | --- |
| 1 | 单选题 | 单选，选项支持随机/轮换/倒序【S3】、分类展示【S3】 | 单选 |
| 2 | 多选题 | 可限选项数 | 多选 |
| 3 | 有序多选题 | 选项有次序含义 | 多选+序 |
| 4 | 开放题（文本） | 短文本，后续编码 | 文本 |
| 5 | 问答题 | 长文本 | 文本 |
| 6 | 数字题 | 数值输入 | 数值范围校验 |
| 7 | 数值分配题 | 总量分配到各选项 | 和校验 |
| 8 | 分配题 | 比例/个数分配 | 和校验 |
| 9 | 排序题 | 选项排序 | 序号唯一 |
| 10 | 表格题（矩阵） | 行×列勾选 | 单元格规则 |
| 11 | 三维表格题 | 三维矩阵【S4】 | 单元格规则 |
| 12 | 哑题 | 不提问仅承载逻辑/计算【S4】 | — |
| 13 | 辅助计算 | 自动计算并展示（如 ARPU→推荐套餐【S2】） | 公式 |
| 14 | 选项关联题 | 选项随前题答案过滤【S3】 | 逻辑 |
| 15 | 甄别题 | 甄别不合格即终止 | 跳转 |
| 16 | 复述确认题 | 机器读出供确认（VIP 语音方案【S1】） | — |
| 17 | 提示/说明题 | 纯展示 | — |
| 18 | 日期题 | 日期选择 | 格式校验 |
| 19 | 预约/回拨题 | 生成预约任务 | 时间 |
| 20 | 结束状态题 | 电话结束状态码采集 | 码表 |

### 9.2 逻辑控制【S3】【S4】

- 跳转逻辑（按答案跳题）、显示逻辑（条件显示/隐藏选项）；
- **多维条件约束**的跳转与逻辑判断【S4】；
- 选项关联、互斥、排斥；选项随机、轮题、倒序【S3】；
- 逻辑关系可**校验、可自动调试**；问卷自动纠错；自定义报错提示【S3】；
- 问卷结构可调整，**执行中途也能调整问卷**（在访员电脑自动更新、答卷自动整合）【S3】【S4】。

### 9.3 配额【S2】

单重配额与交叉配额；自动计算与控制配额执行情况；自动抽取符合配额条件的受访人群；配额实时统计、实时修改【S4】；配额条件可导出【S3】。

### 9.4 题库 / 模板 / 预览【S3】【S4】

常用题库 + 自建题库（拖选使用）；问卷模板与页面风格自定义；所见即所得预览；问卷打印；设计器与调试器可单机运行。

### 9.5 导入导出【S3】

doc / xml / sur 等格式（同类产品仅支持自创格式，此为南康宣传优势）。

---

## 10. 抽样与样本规则（要点）【S3】

| 规则 | 说明 |
| --- | --- |
| 随机生成 | 按规则（号段/区域/分层）随机生成电话号码数据 |
| 条件筛选 | 多字段组合筛选样本 |
| 样本分级 | 样本分级并分配给不同访问员组 |
| 自定义字段 | 客户详细资料字段自定义；字典表 |
| 半年原则 | 半年内已访问样本禁呼 |
| 黑名单 | 全局/项目级禁呼名单 |
| 样本打散 | 打散重排防聚集偏差 |
| 批量修改 | 样本字段批量修改 |
| 一人多号 | 同一样本多个号码按规则排序呼叫【S3】 |
| 结束状态 | 每通电话结束状态可自定义（成功/无人接/忙/传真/拒访/不合格…） |
| 案例参考 | 深圳行为危险因素调查采用 ITACATI + Mitofsky-Waksberg 两阶段随机拨号【S10】 |

---

## 11. 数据模型要点【推定·蓝本】

> 南康内部有《NKP08-S004 数据库设计报告》与 `NK3C_2016-04-13-01.pdm`（PowerDesigner 物理模型）【S6】，未公开。以下为开发蓝本级设计。

**主域划分**：组织权限域 / 项目域 / 问卷域 / 样本域 / 话务域 / 答卷域 / 工单域 / IVR域 / 质检域 / 统计域。

核心表（字段级从略，列关键表与关键外键）：

```
org_domain, org_org, org_group, sys_user, sys_role, sys_resource, sys_acl
prj_project(id, name, type, status, parent_id[父子项目], strategy_id, quota_set...)
qnr_questionnaire(id, version, status), qnr_question, qnr_option, qnr_logic, qnr_quota
smp_pool(id, phone1..phoneN, level, status, owner_group), smp_field(自定义字段), smp_dict
cti_callrecord(id, sample_id, agent_id, project_id, begin, end, result_code, recording_id)
ans_answer(id, callrecord_id, q_no, option_ids, text_value, score)
wfm_workorder(id, type, source_channel, cust_id, flow_id, node, assignee, status)
ivr_flow(id, version), ivr_node, ivr_audio, ivr_message(留言)
qc_task, qc_score(agent_id, scoreitem, value)
rpt_stat_*（统计中间表）
```

关键约定（来自公开规范【S6】）：单主键统一命名 `ID`，18 位精度，传前端转 String（JS Number 仅 16 位精度）；VO 与表字段一一对应；枚举集中管理。

---

## 12. 统计与报表体系【S3】

- **四类统计**：话务统计 / 样本统计 / 单题统计 / 交叉统计；
- **图表**：柱形图、饼状图（三维）等，统计结果实时查看（无需导出）；
- **口径**：单项目、多项目数据统计；配额完成度实时；
- **话费**：自定义话费规则并统计；
- **绩效**：坐席话务量、成功率、质检成绩（绩效考核属 NK3C 全业务流程环节【S4】）；
- **导出**：Excel / SPSS / Quantum / Txt。

---

## 13. NK3C 开发规范（工程实践摘编）【S6】【S7】【S9】【S13】

### 13.1 后端

- 分层职责与禁止事项：nmodel 只放模型/枚举/映射；ndal 只放数据访问（继承 BaseDAO）；业务逻辑与测试放 nserviceprovider；控制器只做参数与结果封装（报错统一在控制器端处理）；
- VOMapping 首参大小写严格（`IvrStage` 不写 `IVRStage`）；
- 传递对象以 `Para` 结尾；可定义 VO 构造函数与填充方法；
- 事务：`startManagedSession(false)`，用完必须关闭；
- 返回统一 `ResultInfo` 封装；框架不支持时用 `setHttpResponse` 返回异常（前端对 responseText 特殊处理）；前端必须处理 `ResultInfo=false` 分支并给出详细错误（历史教训：只提示"提交失败"会增加联调成本【S13】）；
- ID：Long 18 位精度，序列化为 String 再传前端；
- URL 全小写（UNIX 路径大小写敏感）；控制器名与文件名一致；参数用 `@RequestBody`；
- 需求跟踪：南康内部使用 Jira（需求跟踪系统）【S6】。

### 13.2 前端

- 兼容性：IE10+，兼容 IE9；ajax get 请求加时间戳参数兼容 IE 缓存；
- 结构：一个 .vm 对应同名 .js 与 .css；css 在 head 绝对路径引用；js 在 body 底部引用并定义 rootPath；
- requireJS 模块骨架：私有变量（前置下划线）→ 初始化 → 事件绑定 → 执行 → 返回 → 全局函数；js 文件头加 `"use strict"`；
- UI 组件：nkui（南康自研；operamasks-ui 弃用）；复杂页面可用 vuejs；
- 标准管理界面范式：**列表页 + "新增/修改"弹出窗口**（以项目管理页为基准样例【S6】）；
- 页面内加载两种方式：iframe、内部元素（#parse / load）；
- 命名：JS 取值用 id，CSS 用 class。

### 13.3 数据库与中间件

- 三库支持：Oracle / SQL Server / MySQL；MyBatis + Druid；SQL Server 驱动版本走 Maven 属性管理【S9】；
- 建议拼 SQL 的场景放 JDBC 封装（便于调试定位）【S6】；
- 部署：每程序独立 Tomcat；JVM `-Xmx768m -XX:MaxPermSize=160m`；机器最低 2G 内存【S8】。

---

## 14. 各系统最小功能集（MVP）定义

### 14.1 "ITACATI 类"电访调研系统 MVP

| 子系统 | 最小功能（缺一不可闭环） |
| --- | --- |
| 账号权限 | 用户/角色/登录；坐席工号 |
| 项目管理 | 项目 CRUD + 状态机 + 坐席组绑定 |
| 问卷 | 6 种基础题型（单选/多选/开放/数字/排序/表格）+ 跳题逻辑 + 必答校验 + 预览 |
| 样本 | Excel 导入 + 状态码表 + 黑名单 + 分配到坐席组 |
| 呼叫 | 预览呼出（手动点击外呼）+ 通话记录 + 录音 |
| 执行 | 坐席弹屏问卷作答 + 结果码提交 |
| 监控 | 坐席状态墙 + 项目进度（成功/失败计数） |
| 审核 | 答卷列表 + 录音回放比对 |
| 统计 | 话务统计 + 单题频数 + Excel 导出 |

### 14.2 "NK3C 类"云联络中心 MVP（在 14.1 之上增加）

| 子系统 | 追加最小功能 |
| --- | --- |
| 渠道 | 电话（必须）+ 至少 1 个文字渠道（如微信/网页在线）统一排队 |
| IVR | 两级语音菜单 + 转人工 + 留言 |
| 工单 | 工单 CRUD + 3 节点工作流 + 自定义字段 |
| 质检 | 录音抽检打分 + 强制示忙/示闲/签出 |
| 预测外呼 | 自动拨号 + 无效号归类 + 呼损率控制 |
| 多租户/云 | 组织隔离（域）+ 坐席规模弹性扩容 |

### 14.3 分角色最小功能

| 角色 | 最小功能集 |
| --- | --- |
| 系统管理员（域管理员 domainAdmin【S7】） | 组织/用户/角色授权、参数、备份、许可证 |
| 项目经理（组织管理员 orgAdmin） | 项目全流程、问卷、抽样、配额、报表 |
| 督导/组长（组管理员 groupAdmin） | 监控墙、监听插话、强制操作、排班、答卷审核 |
| 访员/坐席（phoneAdmin） | 签入签出、示忙示闲、外呼作答、结果码、预约 |
| 质检员 | 质检任务、录音评分、质检报告 |
| 客服（呼入） | 弹屏、工单录入、知识库检索 |

---

## 15. 风险与合规提示（开发落地必读）【推定】

1. **个人信息保护**：样本含姓名/电话等个人信息，需落实采集告知、最小化、加密存储、访问审计、导出审批（对应《个人信息保护法》《数据安全法》要求）。
2. **外呼合规**：避开禁呼时段（系统支持项目时间段与排班控制【S3】）、尊重拒访与黑名单、控制重拨频次（半年原则即为行业合规实践【S3】）。
3. **录音告知**：通话录音需合规告知。
4. **统计伦理**：抽样方案、配额设置需留存审计记录（配额条件可导出【S3】）。
5. 本文档仅基于公开资料整理，用于学习研究；复刻开发时不得使用南康商标与对外宣称与其产品一致，避免侵权。

---

## 16. 术语表

| 术语 | 释义 |
| --- | --- |
| CATI | Computer Assisted Telephone Interviewing，计算机辅助电话调查 |
| CAPI / CAWI / CAI | 面对面访问 / 网络调查 / 各类计算机辅助调查统称 |
| B/S/S | 浏览器 / 服务器 / 数据库服务器三层结构【S1】 |
| CTI | Computer Telephony Integration，计算机电话集成 |
| IVR | Interactive Voice Response，交互式语音应答（语音菜单） |
| 预测拨号 | 系统按坐席空闲率与接通率算法自动批量拨号并匹配空闲坐席【S2】 |
| 预览呼出 | 坐席先查看样本信息再手动发起呼叫 |
| 呼损率 | 预测外呼中接通后无坐席可分配而被放弃的比例（可控制【S4】） |
| 配额 | 按人口/客户结构设定的样本完成量控制（单重/交叉）【S2】 |
| 半年原则 | 半年内被访问过的样本不再访问的行业规则【S3】 |
| 样本池 | 缓冲待拨样本的独立服务，保障并发与一致性【S2】 |
| 工单 | 呼入/服务事项的电子单据，含自定义字段与工作流【S5】 |
| 分发项目 | 母项目下发子数据库/子公司执行的项目模式【S2】 |
| Quantum / SPSS | 专业统计分析软件数据格式（导出目标格式）【S3】 |

---

## 17. 参考资料（公开来源）

| 编号 | 标题 | URL | 要点 |
| --- | --- | --- | --- |
| S1 | 南康科技电访专家CATI解决方案（CTI论坛，2007-12） | http://www.ctiforum.com/factory/f09/nankang/nankang07_1202.htm | 15 大功能、B/S/S、版本、硬件配置表 |
| S2 | 南康科技外呼解决方案（CTI论坛，2007-12） | http://www.ctiforum.com/factory/f09/nankang/nankang07_1201.htm | .NET/nkframework/SOA、CTI Proxy、消息中心、样本池、分发项目、质检 |
| S3 | 南康官网·数据采集业务（ITACATI 产品页） | http://nk.ddzhuan.cn/product_service/data_collection/data_collection.html | 呼出策略、分权分域、20 题型、样本规则、导出格式、竞品对比 |
| S4 | 南康官网·云联络专家 NK3C 产品页 | http://nk.ddzhuan.cn/product_service/communicate_online_master/communicate_online_master.html | NK3C 定义、云模式、题型清单、外呼优势 |
| S5 | SuperMap 案例：部署 NK3C 云联络专家系统（不动产热线） | https://www.supermap.com/zh-cn/a/case/5_1964.html | NK3C 2017、3C 含义、多渠道、IVR、工单、实时质检 |
| S6 | CSDN 博客：NK3C 开发要点 | https://blog.csdn.net/weixin_34061482/article/details/85836285 | 技术栈、五层分层、前后端规范、数据库设计文档 |
| S7 | CSDN 博客：NK3C 业务权限控制 | https://blog.csdn.net/weixin_34178244/article/details/85836258 | 四级权限 Code、NKZXAgent 软电话部署 |
| S8 | CSDN 博客：NK3C 程序资源占用分析 | https://blog.csdn.net/weixin_33709609/article/details/86370522 | Tomcat/JVM 内存配置 |
| S9 | CSDN 博客：NK3C 框架（MyBatis、Druid）连接 SQL Server | https://blog.csdn.net/weixin_34413103/article/details/86229856 | 驱动与连接池配置 |
| S10 | 百度百科：上海南康科技有限公司 | https://baike.baidu.com/item/上海南康科技有限公司/4968752 | 产品线、沿革、案例（零点/深圳 CDC）、服务体系 |
| S11 | 厦门大学经济与管理教学实验中心 | https://elc.xmu.edu.cn/xmyf.htm | 2004 年建 CATI 实验室采用 ITACATI |
| S12 | 北师大珠海四维创新实验室 | https://fst.bnbu.edu.cn/fpil/info/1414/1002.htm | 32 坐席 ITACATI、功能清单 |
| S13 | CSDN 博客：NK3C 异常处理（前端） | https://blog.csdn.net/weixin_34378045/article/details/85836286 | ResultInfo 错误处理规范 |

> 访问与核对日期：2026-09-17。公开资料截至各原文发布时间，南康现行版本功能可能已有演进。
